## F1 [high] Default host is the owner's: FOUNDRY_HOST unset = molten; FOUNDRY_URL alone ignored; placeholder URL costs 600 s before the first error
VERDICT: corrected
REASON: Evidence holds, two line cites are off. Checked src/hosts/env.ts: hostKindFromEnv returns 'molten' at :49 when FOUNDRY_HOST is unset (:41-50); FOUNDRY_URL is read only in the generic branch at :102; the molten default is 'https://your-server.moltenhosting.com' at :112. Re-ran the probe offline against dist (scratch/skeptic-f1-host.mjs): resolveHostConfig({FOUNDRY_URL:'https://vps.example.com'}) -> kind 'molten', serverUrl = the placeholder; hostKindFromEnv({}) -> 'molten'. `git grep your-server src` -> only env.ts:28,112,118,120 and env.test.ts:75 (which locks the placeholder) — no guard; src/index.ts and src/config.ts:76-79 never check it. The 600 s claim holds: `nslookup your-server.moltenhosting.com` -> NXDOMAIN, so page.goto fails fast, probeJoinState returns 'booting', ensureWorldReady sleeps 5 s and loops until the budget expires; MoltenHost.wake() is a no-op without MOLTEN_MAGIC_URL (src/hosts/molten.ts:63). But the budget line is src/foundry.ts:194 (`wakeTimeoutMs ?? 600_000`), not :197, and the nav-error -> 'booting' mapping is :242-245 (catch at :244, return at :245), not :250-255 (F4 in the same draft already cites :194 — the draft is internally inconsistent). Fix side (config) and shape are right: 'generic' default matches owner decision 1; env.test.ts:37/:75 will need updating.
CORRECTION: Line cites: src/foundry.ts:194 for the 600,000 ms budget and src/foundry.ts:242-245 for the navigation-error -> 'booting' mapping (not :197 / :250-255). Everything else in the finding stands as written.

## F2 [high] Undeclared library surface and broken packaging: no exports/bin/files, main starts a server at import, playwright is a devDependency of the module that imports it, .env resolved relative to dist/
VERDICT: corrected
REASON: Checked package.json: private:true, main dist/index.js, types dist/index.d.ts, no exports/bin/files — correct. src/index.ts:129 is `main().catch(...)` at module top — correct. src/foundry.ts:18 imports 'playwright' and playwright is under devDependencies — correct. src/tools/test-helpers.ts:12 imports vitest and dist/tools/test-helpers.js exists (tsconfig excludes only *.test.*/*.spec.*) — correct. src/config.ts:15 resolves '../.env' relative to dist/config.js — correct. Corrections: (1) `dotenv` is ALREADY a runtime dependency (package.json dependencies: "dotenv": "^16.3.0"), so 'playwright + dotenv -> dependencies' should read 'playwright -> dependencies'. (2) The count is 88 scripts (scripts/*.mjs + scripts/lib) importing dist/foundry.js by path, 85 of which go through scripts/lib/bridge-config.mjs and 86 call `new Foundry(` — the finding's '85' is the bridge-config count, not the by-path count. (3) The devDependency does not block a clone: README:244-246 already documents `npm install` (which installs devDependencies) + `npx playwright install chromium` + `npm run build`, and README:235 explicitly says 'Playwright is a devDependency'; it only bites `npm ci --omit=dev` or a published package. So the fix item 'README says clone and build until published' is already the current README state. Severity stays high only because the missing exports block the sibling contract (owner decision 5), not because a clone fails.
CORRECTION: dotenv is already in dependencies — move only playwright. 88 (not 85) scripts import dist/foundry.js by path (85 via bridge-config.mjs). README:235,244-246 already state the devDependency and the clone-and-build path; a plain clone works today — the packaging defect blocks `--omit=dev`/publish and the sibling import contract, which is what justifies 'high'.

## F3 [high] The rename broke every sibling (battleflow 21 files, fxstudio default path, miscpatches) and e53958e silently dropped magicUrl that battleflow's prod target still passes
VERDICT: confirmed
REASON: Reproduced offline. `ls D:/Workbench/FVTT/Repos/fvtt-mcp-molten5e` -> No such file. `node tools/smoke-saves.mjs --list` in fvtt-mod-battleflow -> ERR_MODULE_NOT_FOUND for D:\...\fvtt-mcp-molten5e\dist\foundry.js imported from tools/harness.mjs (import at harness.mjs:36, hard-coded MCP path at :38); 57 of the 79 top-level tools/*.mjs reference harness.mjs (finding says 56 of 83 — a subdirectory-counting difference, same substance). fxstudio tools/lib/env.mjs:15 defaults MCP_REPO to ../fvtt-mcp-molten5e and classicLevel() at :41-48 does createRequire(join(MCP_REPO,'package.json')) — measured: require('classic-level') from the old path throws MODULE_NOT_FOUND, from fvtt-mcp-dnd5e it resolves; 15 fxstudio files still name the old repo. miscpatches tools/smoke-teleports.mjs:10 imports file:///D:/.../fvtt-mcp-molten5e/dist/foundry.js and :12 hard-codes the path. `git show e53958e -- src/foundry.ts` removes `magicUrl?: string` and the wake code that read it; current src/foundry.ts has zero 'magicUrl' hits; battleflow tools/target.mjs:27-31 (prod branch) still passes `magicUrl: env.MOLTEN_MAGIC_URL` with no `host`, so Foundry.wake() (foundry.ts:164-166) is a no-op — the box is never woken. Re-ran scratch/siblings-census.mjs: 57 tracked files across 6 repos (battleflow, fxstudio, miscpatches, soundscape-sfx, artificer, campaign) name the old repo; 0 name fvtt-mcp-dnd5e (my code/doc split is 39/18 vs the draft's 40/17 — one .json/CLAUDE.md classification, same total). Fix is on the sibling side and proportionate: the sed is the immediate step; the file: dependency depends on F2's exports landing.
CORRECTION: 

## F4 [high] bestiary-entry.mjs bypasses the host seam: hand-parsed MOLTEN_*, a dead magicUrl key, 'DM Assistant', FOUNDRY_HOST ignored
VERDICT: corrected
REASON: Evidence holds exactly: .claude/skills/bestiary-builder/bestiary-entry.mjs:66-72 hand-parses REPO_ROOT/.env, :74-81 builds `new Foundry({ serverUrl: env.MOLTEN_SERVER_URL, magicUrl: env.MOLTEN_MAGIC_URL, user: env.FOUNDRY_USER || 'DM Assistant', ... })`; FoundryConfig (src/foundry.ts:62-95) has `host?` and no `magicUrl` since e53958e, so the key is dead, wake() is a no-op and ensureWorldReady polls up to 600 s (foundry.ts:194); process.env.FOUNDRY_HOST is never read, so the script always targets MOLTEN_SERVER_URL; `git log -- bestiary-entry.mjs` shows only affaf76 and 252cdec (untouched by e53958e). SKILL.md:20-24 says the script exists because two capabilities have no tool. Corrections to the FIX: (1) scripts/lib/bridge-config.mjs exports `bridgeConfig(env, {host})` and `hostFor(env)` only — there is no `loadEnv` (that is F2's proposed client.ts). The short fix is a 2-3 line change, not ~15: keep the :67-72 loop and replace :74-81 with `new Foundry(bridgeConfig(env))` (honours FOUNDRY_HOST via process.env) or add `host: hostFor(env)` and drop magicUrl. (2) Half of the 'right fix' already exists: update-journal with playerVisible:true already sets ENTRY-level ownership via openEntryForHandouts (src/page/journals.ts:175-177,191-193,285-293), and appendPageOwnership (:296-306) keeps a GM-only page GM-only inside an open entry — so 'entry-level ownership on update-journal' is not missing and SKILL.md:20-24 is stale on that point. get-compendium-entry's page side is pack.getDocument(id).toObject() for ANY pack type with fullData returned in full mode (src/page/compendium.ts:198-227, src/tools/compendium.ts:477-487), so a compendium JournalEntry is readable today, though a whole MM lore entry would hit the 20,000-char cap — a PAGE-targeted read is the genuinely missing piece, along with the alphabetical page re-sort and the DOM-based furniture stripping. Severity high is defensible only for the 'broken for any non-Molten clone' clause (serverUrl undefined -> `undefined/join` nav error -> 600 s loop).
CORRECTION: Short fix: replace bestiary-entry.mjs:74-81 with `new Foundry(bridgeConfig(env))` from scripts/lib/bridge-config.mjs (which exports bridgeConfig/hostFor, not loadEnv) — 2-3 lines. Right fix scope: update-journal already opens the entry for a player-visible page (journals.ts:285-293); what is missing is a page-targeted compendium-journal read (whole-entry fullData exists but hits the cap), page re-sort, and the lore-stripping — SKILL.md:20-24 should be corrected accordingly.

## F5 [high] Central error mapper replaces classified errors with canned text and drops the original message; page-side errors cannot escape it
VERDICT: corrected
REASON: Numbers and lines all reproduce: src/utils/error-handler.ts:53-188 classifies by substring (permission :58, connection/timeout/browser/game.ready :74-84, not found/invalid/missing :100-104, scene :147, rollback/transaction :162); formatErrorMessage :195-207 emits message + suggestions and never appends `details`; toUserMessage :249-262 returns raw only for ZodError or the generic catch-all; src/foundry.ts:465 re-wraps every page throw as plain Error. Re-ran scratch/arch-seams-throw-census.mjs: src/page 395 `throw new Error(` / 0 FormattedToolError / 112 literals trip a heuristic; src/tools 43 / 19 / 7. Re-ran scratch/arch-seams-error-mangle.mjs: 11 of 12 mangled, e.g. Actor "Gren" not found -> 'Invalid request or missing data'. Corrections: (1) the 12 cases are synthetic; the raw text a client actually receives is `foundry.call('manageEffect') failed: page.evaluate: Error: <msg>` plus the in-page stack (scratch/pin-6.0.3-verify-2026-09-20.log:47-51), so fix (1) 'append the raw message' must also strip the `page.evaluate: Error:` prefix and the stack lines or the appended text is noise. (2) Fix (2) as written cannot work: measured with playwright on about:blank (scratch/skeptic-f5-evaluate-error.mjs) — an Error thrown inside page.evaluate arrives as a plain Error whose `code` is undefined and whose `name` is folded into the message as a 'PageError:' prefix; a PageError{code} must carry the code in the error name or a message prefix, not a property. (3) Severity: under the brief's own rubric (high = blocks a 3.0 goal / a clone / a measured cost >= 10% of a budget) this is medium — a real correctness cost with a clear fix; no 3.0 goal or clone is blocked and no cost was measured. Fix side (tool) is right.
CORRECTION: Fix (1): append the raw message after stripping the `page.evaluate: Error:` prefix and the in-page stack that Playwright folds into it. Fix (2): encode the page-side error code in the Error's name or a message prefix — custom properties do not survive page.evaluate (measured: code undefined, keys ['log','name']). Severity: medium by the brief's rubric unless the synthesis measures a retry cost.

## F6 [high] search-compendium-creatures: no name facet, silent arg strip, default limit 500 guarantees a mid-JSON cut
VERDICT: corrected
REASON: Core claim holds: ListCreaturesByCriteriaSchema (src/tools/compendium.ts:55-149) has no name/query key; zod 4.4.3 strips unknown keys (re-ran: z.object({limit}).parse({query:'goblin'}) -> {limit:500}); body starts "criteriaDescription":"no criteria"; engine passesPostFilters honours args.name at src/page/compendium-facets.ts:289-296 and the facade call at :520-528 never forwards it; trailing counts at :534-541; spells/items default limit 50 (:224, :279). Numbers: the captured body has 52 complete hits + 1 torn (53rd "id":), marker is 73 chars, avg hit 377.6 chars (not 379), compact hit 131 chars (-65.3%) -- all fine. Three errors: (1) the .strict() census is inverted -- `grep -rn '\.strict()' src/tools` returns ZERO hits; scene.ts (13) and placeables/region.ts (4) are `.passthrough()` sites on nested Foundry-data sub-schemas, i.e. the opposite (accept unknown keys); 0 of 151 tool schemas refuse unknown keys today. (2) raw is 186,538 chars (20,000 + the marker's 166,538 omitted); 186,611 adds the 73-char marker to the raw; implied hits ~492. (3) the fix's compact hit {id,name,pack,cr,type,size,spells?,legendary?} drops `uuid`, which table-builder SKILL.md:75 tells the model to take from search-compendium-creatures and scratch/skills-notes.md §3 lists as passed onward -- and it contradicts F9's unified shape {id,name,uuid,pack,img,facets}; the shape is also pinned in docs/compendium-lookup-plan.md:36, stat-block-builder SKILL.md:35 and the 500 default in compendium.test.ts:311,335 (so 'no skill/doc change' is not true). Adding .strict() to every schema emits "additionalProperties":false (verified via z.toJSONSchema io:'input'): 28 chars x 151 = ~4.2k chars (+1.5%) on the eager list -- acceptable but unstated. Severity high stands: the default call cannot fit under the 20k cap (500 x 378 chars).
CORRECTION: Evidence: 'no tool schema uses .strict() (0/151); scene.ts (13) and region.ts (4) use .passthrough() on nested sub-schemas'; raw 186,538 chars (= 20,000 + 166,538), ~492 hits x 378 chars, compact 131 chars/hit (-65%). Fix: add `name` and forward it; default limit 50; .strict() on top-level schemas only (costs ~4.2k chars of additionalProperties:false on tools/list); use ONE compact hit shape shared with F9 that KEEPS uuid (table-builder :75) -- e.g. {id,name,uuid,pack,img?,facets}; update compendium.test.ts:311/335, docs/compendium-lookup-plan.md:36 and stat-block-builder SKILL.md:35 in the same commit. Estimate ~2 hours, not 1.

## F7 [high] list-actor-ownership emits 7 user rows per actor for all 168 actors (227,662 chars); explicit-only shape is 12,240
VERDICT: confirmed
REASON: Checked src/page/ownership.ts:51-115: `for (const user of users.filter(u => !u.isGM))` pushes a 7-field row for every actor x user regardless of `hasExplicit`. Captured body (scratch/bodies-2026-09-20/list-actor-ownership.txt): marker '207662 chars omitted' -> raw 227,662 exactly; 14 complete actor records, each with 7 user rows (Andrew, Anthony, Open Player 1/2, PC Assistant, Robert, Tom = the 7 role-1 users in list-users.txt; the 3 role>=3 users are isGM), 7 fields per row; 1 of 14 actors (BF Test PC Attacker) has any explicit entry; avg 1,358 chars/actor (x168 actors from list-actors.txt = 228,144, vs measured 227,662); avg user row is 173.6 chars (the '~190' is ~8% high, immaterial). Compact {id,name,default,explicit?} = 71.8 chars/actor -> ~12.26k for 168 (script perf-results-cap.mjs re-run: 12,637 with the success envelope; finding 12,240 -- all within 3%). `grep -rl list-actor-ownership .claude/skills/` = none (only set-actor-ownership, 2 skills). Fix is page-side src/page/** = tool side per design.md §2.1, lossless because a non-GM's testUserPermission is explicit ?? default; actorIdentifier/playerIdentifier already exist (ownership.ts:53-56) to gate the per-user matrix without a schema change. Existing unit file src/tools/ownership.test.ts:215-233 covers the tool; page-side test needed.
CORRECTION: 

## F8 [high] capResponse cuts mid-JSON and always drops the trailing counts; a record-level cut keeps them for ~50 lines
VERDICT: confirmed
REASON: src/index.ts:30-34 is capResponse (slice at maxChars + marker); config.ts:68 default 20,000; marker measured 73 chars in both capped bodies. Grep for readers of tool text: only scripts/measure-tool-results.mjs:125 and scripts/verify-toolsets.mjs:114 read content[0].text and neither JSON.parses it (verify-toolsets regex-matches an error string); no tests/ or .claude/skills file parses tool JSON -- so no programmatic breakage, model-only. Creatures body: 52 complete + record 53 torn; the trailing keys totalFound/criteria/note are at compendium.ts:534-541 and list-actors' total/filtered at actor.ts:313-314 (the block is :306-315 -- cite off by 3 lines, trivial). Re-ran scratch/perf-results-cap.mjs: output identical to the saved .out -- creatures 51/493 hits in 19,826 chars with totalFound+note kept, ownership 14/168 in 19,209 (vs today's 14 complete + 1 partial + lost count), markdown list cut at the last newline with '72 more line(s) omitted'. Fix is proportionate and correctly placed (index.ts, before the JSON.stringify at :85 so the object is available); note capResponse is unexported with no unit test today, so 'unit test' implies extracting it to src/utils. Top-level-array only -- nested fat dumps (get-actor) still fall back to the character cut, which the finding does not claim to fix.
CORRECTION: 

## F9 [high] List/search bodies carry fields no skill reads; a compact projection saves 32% of the 24-call baseline (48,884 chars)
VERDICT: corrected
REASON: Re-ran scratch/perf-results-projection.mjs: the table is byte-identical to the saved .out (list-items 16,392->7,986, list-scenes ->2,873, spells ->6,568, search-compendium ->4,825 with description "" on 42/42, get-world-info ->354, total 154,534->105,650 = -32%/-48,884); search-compendium returns pack:{id,label} while the faceted three return pack:"<id>"+packLabel (confirmed in the bodies). Field grep over .claude/skills: hasImage, folderId, pageCount, hotbar are read by no skill (true). But two rows were computed for a shape the fix does NOT propose and that skills DO read: (1) get-world-info's `automation` block (599 of 1,805 chars, 23 keys) is explicitly read by start-session SKILL.md:47 ('the same result carries an automation block') and session-audit SKILL.md:194-197 ('get-world-info carries an automation block ... fold the ones that change the encounter math'); dropping only description (760) + background (49) gives 1,805->967 (-46%), not ->354 (-80%). If F23 also moves configure-dnd5e-settings out of the always-on set, get-world-info becomes the only always-on read of those switches. (2) the spells number is variant A (drops uuid, img, type, packLabel, criteria, note) while the fix's unified shape {id,name,uuid,pack,img,facets} keeps uuid and img -- table-builder SKILL.md:75 says 'Get the uuid from search-compendium-items / -creatures / -spells' and the finding's own source scratch/skills-notes.md §3 lists uuid and img as passed onward; with the unified shape spells go 14,979->12,924 (-14%), and the creature row shrinks proportionally. Corrected 24-call total ≈ 112,619 (-27%, -41,915) instead of 105,650 (-32%, -48,884). Also the current hit shape is documented in stat-block-builder SKILL.md:35 and docs/compendium-lookup-plan.md:36 (skill + doc edits ride along). Severity high still holds (-27% ≈ 11.6k tokens per baseline).
CORRECTION: get-world-info: drop description + background only (1,805 -> 967, -46%); keep `automation` (read by start-session :47 and session-audit :194-197). search-compendium-spells with the proposed unified hit shape {id,name,uuid,pack,img,facets}: 14,979 -> 12,924 (-14%), not 6,568. Corrected 24-call baseline: 154,534 -> ~112,600 chars (-27%, ~41.9k chars ≈ 11.6k tokens), and M2's definition of done should read '≤ ~112,600', not '≤ 105,650' -- unless the fix explicitly drops uuid/img from hits and re-points table-builder :75 / stat-block-builder :35 / docs/compendium-lookup-plan.md:36 in the same commit.

## F10 [high] 67% of tools/list is prose; half of description bytes are field-level detail; tool descriptions carry skill doctrine
VERDICT: corrected
REASON: Re-ran scratch/perf-schema-decompose.mjs: 283,948 chars; leaf .describe() 122,631 (43.2%, 1,209 fields); descriptions 68,655 (24.2%); structural 73,193 (25.8%) -- exact; 43.2+24.2 = 67% prose holds. Re-ran perf-schema-descriptions.mjs: 38 leaves >= 300 chars (14,719), field-level sentences 214/552 = 33,661 (49.2%), 15 tools >= 70% with the desc >= 500 gate (add-item 1,583/1,972, update-actor 1,574/1,710 ... search-compendium-spells 565/567), boilerplate 6 sentences 1,131 (1.7%) -- all reproduce. Zero-leaf tools: `grep -c '.describe(' src/tools/dnd5e/npc.ts pc.ts` = 0 and 0; author-npc 1,193 / create-pc 1,652 / create-pc-from-prefab 965 / level-up-pc 1,032 description chars (965 is under the '1.0k' floor, trivial). The three quotes (author-npc 'LAST-RESORT path in the §6 ladder' + 'ask before authoring', import-item WORKFLOW with pack ids, duplicate-actor 'THE sandbox path') exist verbatim. Two corrections: (1) '48 descriptions carry a judgment word' is the raw regex count from skills-tooldesc.mjs (re-run: 48); 26 of the 48 match ONLY 'never' inside contract sentences ('Missing ids are reported, never fatal', 'a partial change never wipes the rest', 'base-actor edits never reach tokens'), and of the other 22, ~7 are dnd5e/Foundry vocabulary (OWNER permission level, 'ALWAYS-PREPARED spell', defaultOwnership:'owner') and ~5 are factual house-module references; genuine prefer/ask-first/ladder doctrine is ~8 tools (author-npc, create-actor-from-compendium, import-item, add-item, add-feature, update-actor-item, create-item, duplicate-actor). (2) the fix's 'Expected -55-80k chars' exceeds what the stated budget can deliver by construction: capping every leaf at 120 chars (237 of 1,209 exceed; median 58, p90 179) saves 22,305 and every description at 400 (67 of 151 exceed) saves 21,855 -> ceiling -44,160 (-15.6%, -> 239,788) even with zero-content cuts; the draft's own §3.4 row says -30-50k, and -55-80k double-counts the '15 descriptions rewritten' saving that already sits inside the <= 400 cap. Side: doctrine out of descriptions lands in skills, so it is tool+skill. Severity high holds for eager clients only -- the brief notes Claude Code defers schemas, so this is not the owner's measured cost.
CORRECTION: Evidence: '~8 tool descriptions carry prefer/ask-first/ladder doctrine (author-npc, create-actor-from-compendium, import-item, add-item, add-feature, update-actor-item, create-item, duplicate-actor); the 48 regex hits include 26 contract-only "never"s and ~7 dnd5e/Foundry terms'. Fix expectation: the leaf <= 120 / description <= 400 budget has a mechanical ceiling of -44k chars (-15.6%, tools/list -> ~240k); state 'expected -30-44k' and let M7's <= 230,000 target rest on F22/F23 (identifier dedup, toolset split) for the remainder. Side: tool + skill (the ladders move into stat-block-builder / physical-item-builder / _shared/authoring-policy.md).

## F11 [high] Fewer tools does not mean fewer bytes: a lossless kind x op union costs +4.5%; in Claude Code the CRUD consolidation pays through the name list (82 CRUD names = 53.1%); the sandbox registration can be project-scoped today
VERDICT: corrected
REASON: Re-ran `node --import ./scratch/perf-schema-hook-register.mjs scratch/perf-schema-shapes.mjs`: every figure reproduces exactly (today 78,572; (a) 3,521 = 4.5%; (b1) 62,334 = 79.3%; (b2) 82,130 = 104.5%; (c) genuine 58,121 / 77,932; whole list (a) 208,897, (b2) 287,506 = 101.3%; deferred describe payload 61,977+7,289+8,886 = 78,152). `scratch/siblings-names-cost.mjs` reproduces 302 names = 11,147 chars / 3,096 tokens. My scratch/skeptic-c3-names.mjs reproduces 82 regex-CRUD names = 3,082 of 5,800 prefixed chars = 53.1%. `TOOLSETS.world` (src/toolsets.ts:19-28) = 8 names / 299 chars with the local5e prefix. ~/.claude.json: 31 project entries, 0 with project-level mcpServers (user-level: foundry-molten5e, foundry-local5e, artificer). Sibling census: fxstudio uses get-world-info+disconnect-bridge, miscpatches disconnect-bridge; battleflow names 5 tools but get-combat-stats/level-up-pc/manage-effect are prod-side reporting or 'tried and rejected' comments, so `world` fits the three module repos. Four things are wrong or missing: (1) the b2 union as measured (`scratch/perf-schema-shapes-placeables-b2.json`) has no top-level `type:"object"`; the SDK's ToolSchema requires `type: z.literal('object')` (node_modules/@modelcontextprotocol/sdk/dist/esm/types.js:1240-1246) — +16 chars, immaterial to the numbers but the implementer must wrap it. (2) The claim that consolidation 'pays through the name list' in Claude Code ignores that Claude Code's on-demand load injects the WHOLE union schema into context: a b2 placeables tool costs 64,619 chars ≈ 17.9k tokens on first use vs ~1,779 chars per tool today (62,270/35); against the placeables name saving of 1,251 chars per registration per prompt (2,502 with two ≈ 695 tokens) the break-even is ≈ 22–26 prompts inside one conversation — net-negative for short sessions unless the prose diet lands first (which the fix already requires). (3) '82 CRUD names' is the regex count (perf-schema-crud-all.mjs: 31 'families' incl. 10 singletons such as create-pc, create-group, list-users, list-compendium-packs, delete-journal-page) that no consolidation merges; the brief's family plan (76 tools → 10 family tools, scratch/skeptic-c3-consol.mjs) saves 2,427 of 5,800 chars per registration = 41.8% ≈ 1.35k tokens/prompt with two registrations — consistent with §4's '≈ −43%' but not with '82 names'. (4) '16 projects' is 31 entries in ~/.claude.json (16 existing dirs). Fix correction: `FOUNDRY_TOOLSETS=world` on the sandbox registration is right for fxstudio/battleflow/miscpatches only — in THIS repo the sandbox registration must keep the full surface because the release DoD includes 'a Claude Code smoke after restart (new tool schemas need a restart)' (HANDOFF.md:149) and CLAUDE.md step 3 drives a real MCP call on foundry-local5e; HANDOFF.md:166 already lists the per-project demotion as an open owner decision.
CORRECTION: Keep the byte numbers. Rewrite the Claude Code clause: 'the consolidation saves ≈ 2,427 name-chars per registration (41.8%, ≈ 1.35k tokens/prompt with two registrations) for the brief's 76→10 family plan, but a b2 union loads its whole family schema on first use (placeables 64,619 chars ≈ 17.9k tokens vs ~1.8k chars per tool today), so it is net-positive only in conversations longer than ≈ 22–26 prompts — the prose diet must land BEFORE or WITH the union so the family's loaded bytes end ≤ today's.' Note the union must be wrapped in a top-level `type:"object"` (SDK ToolSchema, types.js:1240). Change '82 names' to 'the regex-82 includes 10 singletons no consolidation merges'. Change '16 projects' to '31 entries (16 existing dirs)'. Change the fix to: project-scope `foundry-local5e` with `FOUNDRY_TOOLSETS=world` in fxstudio/battleflow/miscpatches only; this repo keeps the full sandbox surface (HANDOFF.md:149 Claude Code smoke).

## F12 [high] Generic host has no file plane although Foundry's own FilePicker upload works on every host through the bridge — 9 tools and 7 of 17 skills dead on generic
VERDICT: corrected
REASON: Cites hold: src/hosts/generic.ts:28 `readonly files = null`; the 9 plane handlers (src/tools/assets/index.ts:318-664) each `return this.notConfigured(...)` when `host.files` is null (asset-url at :305 is pure mapping, so 9 of 10); chat.ts:400-402 refuses image upload and :510-512 skips/refuses the remote copy; `grep -rn FilePicker src` → 0; 7 of 17 skill dirs call upload-asset/upload-asset-tree/list-assets (bestiary-builder, cards-builder, playlist-builder, scene-builder, soundscape-builder, token-cutout, tom-cartos-import); the 9 definitions sum to 7,664 chars (per-tool JSON, 7,674 as an array) = 2.7% of 283,948; list-assets/asset-info/download-asset descriptions (:218-241) lack the 'Needs the host's file plane configured' sentence the six write tools carry (asset-info never mentions the plane at all). The technical premise checks out against the sandbox install (C:/Program Files/Foundry Virtual Tabletop/resources/app, 14.368): client/applications/apps/file-picker.mjs exposes static browse (:372), createDirectory (:400), upload (:434, POST /upload with FormData) and nothing for delete/move/copy; dist/files/files.mjs `manageFiles` handles only browseFiles/createDirectory/configurePath; dist/server/views/upload.mjs gates on FILES_UPLOAD (default ASSISTANT, common/constants.mjs:1327-1332), so a GM bridge user can upload on any host. But the fix must carry constraints the finding omits: (a) `Files.upload` refuses any extension outside UPLOADABLE_FILE_EXTENSIONS (images/audio/video/csv/json/md/pdf/tsv/txt/xml/yml/fonts/textures/graphics — no .html), so `export-chat-log format:html remotePath` (chat.ts:160-180) cannot go through this plane; (b) it sets overwrite = isMedia && !package.json, and LocalFileStorage.upload throws 'You may not upload non-media files which would overwrite an existing file' — `upload-asset overwrite:true` on .json/.md/.txt fails on the FilePicker plane; (c) createDirectory requires isAdmin (ASSISTANT+) and does not create parents, so ensureParents must walk segment by segment; (d) `read`/download has no FilePicker API — it is an HTTP GET of the public URL (host.publicUrl), which works everywhere; (e) bytes traverse the CDP evaluate channel as base64, so upload-asset's promise that 'large media bypass the bridge entirely' (index.ts:246-249) must be reworded for the default plane (no server-side size limit: express `fileUpload({defParamCharset:'utf8'})` sets none); (f) design.md §2.6 (:98-100) binds 'a host without a file plane makes the asset tools say so, by name' — the fix amends a binding sentence, i.e. an owner decision, not only a tool change. Wording: '7 of 17 skills dead' overstates — scene-builder (SKILL.md:76-80) and playlist/soundscape-builder also offer 'pick an already-uploaded path', which on generic degrades to a hand-typed Data-relative path; tom-cartos-import, token-cutout, bestiary-builder and cards-builder are genuinely blocked on upload.
CORRECTION: Keep severity and side. Evidence: '9 tools return filesNotConfigured; 4 skills blocked (tom-cartos-import, token-cutout, bestiary-builder, cards-builder), 3 degraded to hand-typed paths (scene-, playlist-, soundscape-builder)'. Fix: bridge FilePlane = browse/createDirectory/upload via FilePicker (GM/Assistant user), read = HTTP GET of publicUrl; remove/move/copy unsupported by name; document the plane's limits in the tool descriptions — uploadable extensions only (no .html: export-chat-log's remote copy stays plane-dependent), no overwrite of non-media files, bytes through the bridge (drop 'bypass the bridge entirely' from upload-asset). Record that design.md §2.6's 'a host without a file plane says so' becomes 'every host has the bridge plane; molten/local add a fast path'.

## F13 [high] The skills have no install path — a stranger gets 151 tools and zero skills unless Claude Code runs inside this repo
VERDICT: corrected
REASON: README.md: `.claude/skills` appears 0 times; 'skills' as a word 4 times (:11,137,195,314) — the finding's 6 counts the substring 'skill'; :11 'Paired with its bundled **skills**' ✓; :259-291 'Wire into Claude Code' registers via ~/.claude.json / .mcp.json with absolute paths and says the server may launch from any directory ✓; the Repository layout block (:216-228) does not list `.claude/skills/` at all; HANDOFF.md:111 calls the skills 'the sell' ✓; package.json has no `files` field and is `private: true`. Claude Code loads project skills from the cwd's `.claude/skills` and personal ones from `~/.claude/skills`, so a stranger registering the server from another directory gets no skills — the finding is real. The fix's 'copy' option is wrong for one skill: `.claude/skills/bestiary-builder/bestiary-entry.mjs:35-37` computes `REPO_ROOT = join(__dirname,'..','..','..')` and imports `dist/foundry.js` from there, so a copy into ~/.claude/skills resolves REPO_ROOT to `~` and breaks; a symlink/junction works (Node resolves realpath for import.meta.url) or the script must locate the repo another way. Also `_shared/authoring-policy.md` must travel with the skills — five SKILL.md files link `../_shared/authoring-policy.md` (cards-builder:30, journal-builder:31, pc-builder:49, physical-item-builder:26,107). `package.json` `files` is moot while `private:true` (F2 says clone-and-build until published).
CORRECTION: Evidence: 'skills' 4 mentions (the word), `.claude/skills` 0, and the Repository layout (README:216-228) omits the skills dir. Fix: README start step 'install the skills' = symlink/junction `.claude/skills` (or each skill dir plus `_shared/`) into the project's `.claude/skills` or `~/.claude/skills`; a plain copy breaks bestiary-builder (bestiary-entry.mjs:36 REPO_ROOT relative to the skill dir) unless that script takes the repo from an env var (`FVTT_MCP_REPO`) — fold that into F4's bridge-config fix. Drop 'package.json files includes the skills dir' until the package is published.

## F14 [high] Registration name spelled three ways; start-session hard-codes mcp__foundry-molten5e__; start-session and chat-and-narration name their host, env vars and registration
VERDICT: corrected
REASON: Cites hold: README.md:268 `foundry-prod` / :273 `foundry-sandbox`; .mcp.json.example:4 `foundry-molten5e`; start-session/SKILL.md :16 'Bring the Molten-hosted Foundry world up', :24-25 MOLTEN_MAGIC_URL/ADMIN_KEY/WORLD_ID, :27 'wake the EC2 box', :40 `mcp__foundry-molten5e__get-world-info`, :58 `mcp__foundry-molten5e__get-current-scene`, :73-75 MOLTEN_* again; chat-and-narration/SKILL.md :53 and :58 `embed: "webdav"` ('the default' — stale: chat.ts:39-41 defaults to 'upload' with webdav an alias), :83 MOLTEN_WEBDAV_PASSWORD, :90 'Molten performance drag'; fxstudio CLAUDE.md:98 and tools/lib/foundry.mjs:7 pin the names. `git grep -l` for the two names in tracked files: start-session/SKILL.md, .mcp.json.example, HANDOFF.md, docs/dnd5e-6.0-compat-review.md, docs/local-sandbox.md, docs/plan-2.2-hosts.md, scripts/deploy-house-module.mjs. One line cite is wrong: docs/local-sandbox.md names the pair at :139-140 and :184, not :143-146. Two omissions: start-session's front-matter description (:9 '(Magic-URL wake → world launch)') is the part loaded into every prompt, and :45 hard-codes the `MCP-Claude` user name (ties to F15); the tool side keeps 'over WebDAV' in chat.ts:23 and :91 (the `images` description), so the skill-side 'embed upload' rewrite should land with that description fix. Fix nuance: 'skills never carry a server prefix' is right, but with two registrations a bare `get-world-info` is ambiguous (that is exactly why start-session hard-coded prod) — the rewrite must say 'on the registration the user names (or the only one registered)'.
CORRECTION: docs/local-sandbox.md:139-140,184 (not :143-146). Add start-session :9 (front-matter, in every prompt) and :45 (`MCP-Claude`). Fix wording: 'skills name tools bare; when more than one Foundry is registered the skill calls the registration the user named (prod by default in start-session's prose, never by literal name)'. Land chat.ts:91 'over WebDAV' → 'through the host's file plane' in the same commit.

## F15 [high] Bridge-user story is one clause: no create-a-GM-user step, no role check at connect, two default names; 'DM Assistant' ships in the disconnect-bridge description
VERDICT: confirmed
REASON: Checked: README.md:162 'joins as a dedicated GM user' and :238 'plus a dedicated **passwordless Foundry user**' are the only story; :325 says FOUNDRY_USER defaults to MCP-Claude; no numbered create-the-user step in README, .env.example (:35-38 only names the variable) or docs/local-sandbox.md. `grep -n 'isGM\|role' src/foundry.ts` → 0 hits: joinWorld (:347-383) never reads game.user.role; page-side GM gates for writes exist only at src/page/dnd5e/calendar.ts:164-165 and settings.ts:99-101 (chat.ts:61 and macros.ts:104 are different checks). Defaults: src/hosts/env.ts:92,103,113 `'MCP-Claude'` (finding says :104 — off by one) vs `'DM Assistant'` at scripts/reload-clients.mjs:33, verify-actor-export.mjs:40,47, verify-receipt-settings.mjs:49, verify-soundscape-tooling.mjs:48,55 and .claude/skills/bestiary-builder/bestiary-entry.mjs:77 — 7 sites, exactly as listed; src/tools/bridge.ts:9,39 ('Log the DM Assistant bridge user out'), src/registry.ts:150, src/tools/registry.test.ts:80, src/tools/scene.test.ts:182,187; dist/tools/bridge.js:26 ships the phrase in the advertised description. Fix is on the docs/tool side and proportionate: 'warn/refuse below Assistant GM' matches the code's own `isGM` gates (Foundry isGM = role ≥ ASSISTANT; FILES_UPLOAD default ASSISTANT); scripts/lib/bridge-config.mjs:35-53 already routes through resolveHostConfig, so the four scripts and bestiary-entry can take the default from it (overlaps F4).
CORRECTION: 

## F16 [high] 26% of the skill-description budget is non-triggering boilerplate; six rewrites project 15,382 -> 7,417 chars (-52%, ~-2.2k tokens per prompt)
VERDICT: corrected
REASON: Re-ran all three scripts: skills-desc-boilerplate.mjs -> total=15382, composes=1818, ownership=2157 => 3975 (26%), journal-builder 46 / stat-block-builder 43 / physical-item-builder 39 / table-builder 34; skills-desc-budget.mjs -> pc-builder 1317->596, session-audit 1153->604, tom-cartos-import 1057->460, physical-item-builder 1036->508, playlist-builder 1011->544, stat-block-builder 992->454, six 6566->3166 ratio 0.482, six-only 11982, all-17-at-ratio 7417, tokens 4273/3328/2060; skills-desc-check.mjs -> 8+9+5+7+6+5 = 40 quoted triggers, 0 missing. Independently re-extracted the 17 live SKILL.md front-matter descriptions (own script, YAML folded-scalar join): every length matches skills-matrix.json byte-for-byte, total 15,382. The bodies' first paragraphs do already carry the 'Tools used' lists and the ownership line (checked journal-, stat-block-, physical-item-, table-, pc-, playlist-builder, session-audit, tom-cartos-import). Fix side skill is right; ~1 hour is proportionate. What does not hold: the headline '-52% / 7,417 / ~-2.2k tokens' is not a measurement, it is the six-longest-descriptions' ratio (0.482) applied to the 11 descriptions nobody has rewritten. By the script's own boilerplate metric those 11 average ~21% boilerplate vs 31% for the six (start-session 0%, chat-and-narration 6%, session-scribe 13%, scene-builder 14%), so the same ratio is optimistic. The only measured saving is six-only: 15,382 -> 11,982 = -3,400 chars (-22%, ~-945 tokens per prompt). Minor: the fix says 'drop pc-builder's DDB explanation from the description' but the proposed pc-builder text in skills-desc-budget.mjs still carries a one-clause version of it ('that path is removed; the skill refuses and rebuilds...').
CORRECTION: Title/evidence: 'six rewrites measured 15,382 -> 11,982 chars (-22%, ~-945 tokens per prompt); applying the same 0.482 ratio to the other 11 would reach ~7,417 (-52%) -- a target, not a measurement, and likely optimistic because those 11 carry less boilerplate (mean ~21% vs 31%).' Keep severity high on the measured 22% of the description budget. Reconcile the fix text with the proposed pc-builder description (it still explains the DDB refusal in one clause; either keep that clause deliberately or trim it).

## F17 [high] Three skill steps read fields no tool returns (list-scenes flags/darkness, get-current-scene darkness); list-scenes prints a background path no skill reads
VERDICT: corrected
REASON: Checked: tom-cartos-import/SKILL.md:117-120 dedups on flags['tom-cartos-import'].sourceId 'via list-scenes'; soundscape-builder/SKILL.md:49-51 reads 'the darkness level ... weather' from get-current-scene / list-scenes. src/page/scenes.ts:367-396 listScenes returns id, name, active, dimensions, gridSize, background, walls, tokens, lighting, sounds, navigation -- no flags, no darkness; src/page/scenes.ts:322-360 getActiveScene returns no darkness/weather either, and a grep of src/page + src/tools finds no read path that returns environment.darknessLevel (only the update-scene write knob at scenes.ts:1174). Captured bodies confirm (list-scenes.txt: name/id/WxH/grid/background only; get-current-scene.txt: hasBackground, elements, tokens). TOM_CARTOS_FLAG_SCOPE = 'tom-cartos-import' hard-coded at scenes.ts:280 (used by region.ts:752 for remap). tom-cartos :184 and :212 hand-pass the flag; :316-318 says create-scene 'stamps flags'. Corrections: (1) 'src/tools/scene.ts:834-840 likewise' is get-current-scene's formatSceneResponse, not list-scenes -- the list-scenes handler is scene.ts:641-655 (it prints name, id, [active], WxH, grid, background). (2) Background share: 49 of the 50 scenes carry a background line; those lines total 6,475 chars = 67% of the 9,698-char body, not 6,825 -- 6,825 is perf-results-projection.mjs's total saving, which also rewrites 'px, grid ' to ' g' on every line. (3) The fix's '(get-current-scene/update-scene own it)' is false today: the page returns the path (scenes.ts:333) but formatSceneResponse reduces it to hasBackground (scene.ts:838), so list-scenes is the ONLY read tool that returns a scene's background path, and soundscape-builder:51 ('read the background image directly') needs one. Dropping it from list-scenes without adding it to get-current-scene (or a fields/include selector) breaks that step. (4) 'tool stamps it' is half right: create-scene only merges the flags the caller passes (scenes.ts:556-559), but the region provenance flag IS auto-stamped by sidecarRegionToV14 (scenes.ts:286-300) -- :317 is correct for regions, wrong for the scene flag. (5) By the brief's severity rule the byte cost is 6,475 / 154,534 = 4.2% of the results baseline (medium); 'high' rests on the section-2.1 contract break (two skill steps that cannot execute), which is real. Fix side tool is right; half a day is proportionate.
CORRECTION: Evidence: cite src/tools/scene.ts:641-655 for list-scenes and :830-857 (hasBackground at :838) for get-current-scene; '49 background lines = 6,475 chars = 67% of the 9,698-char list-scenes body'. Fix: add background (path) to get-current-scene in the same commit that drops it from list-scenes, or gate it behind an include/fields flag on list-scenes -- otherwise no read tool returns a scene's background path and soundscape-builder:51 dead-ends. Narrow the provenance claim to 'the scene flag is hand-passed (:212); the region flag is auto-stamped (scenes.ts:286-300)'. Severity: high on the contract break only; the byte cost is 4.2% of the baseline.

## F18 [medium] search-compendium walks 28 packs of every document type sequentially (7.8-13.2 s); the faceted tools use dnd5e's parallel, type-filtered fetch (0.5-0.6 s)
VERDICT: corrected
REASON: Wall times reproduce: baseline 7,826 ms (13,200 first run) and the reviewers' own live profile scratch/profile-search-compendium-2026-09-20.txt (12,103 ms of getIndex + 168 ms search). Cites hold: src/page/compendium.ts:96-104 filters game.packs minus SRD minus Scene (28 of 30 per bodies/list-compendium-packs.txt: 11 Item, 6 Actor, 4 RollTable, 4 JournalEntry, 2 Macro, 1 Adventure), :108-112 'for ... if (!pack.indexed) await pack.getIndex({})' in series; installed dnd5e.mjs:95936 pushes system.container/identifier/source; CompendiumBrowser.fetch at 39901-39947 filters to one document class and awaits Promise.all over per-pack getIndex; compendium-facets.ts:362-378 calls it; premium set lives in src/utils/compendium-sources.ts. What does not hold is the attribution. The profile ran the tool's exact loop and shows: 23 of the 28 packs cost 0 ms (the 7 printed are marked 'already indexed', including 10 of the 11 Item packs -- DMG.features/bastions, HoF.items, ...), the 4 RollTable packs cost 675+169+155+139 = 1,138 ms, and ONE pack -- dnd-dungeon-masters-guide.equipment, 548 entries -- cost 10,965 ms of 12,103 (91%). So 'all 11 Item packs un-indexed at join' is contradicted by the capture, and the fix as written barely moves the number: Promise.all is bounded by that single 10.9 s call; the Actor+Item allow-list keeps DMG.equipment (an Item pack) and removes only the 4 table packs (~1.1 s, ~9%). The faceted tools were fast because their Actor / PHB.spells packs were already warm, not because of Promise.all. The warm search-compendium is 168 ms -- on par with faceted. The one part of the fix that addresses the measured cost is the 'optional' background index warm-up. Still unmeasured: whether DMG.equipment is intrinsically slow or simply the first cold getIndex of the session paying a one-time cost (it is the first un-indexed pack in game.packs order), which needs one live run with the loop order reversed.
CORRECTION: Title: 'search-compendium's first call pays ~11-12 s of cold getIndex, 91% of it one pack (dnd-dungeon-masters-guide.equipment, 548 entries, 10,965 ms); 23 of the 28 packs are already indexed at connect; the warm call is 168 ms.' Drop 'all 11 Item packs un-indexed at join'. Fix: promote the index warm-up (after injectBundle, or a lazy first-call prefetch) to the primary fix; the type allow-list saves ~1.1 s (the 4 RollTable packs) and Promise.all saves nothing while one call dominates; add the live check that reorders the loop to separate 'slow pack' from 'first-cold-call cost'. Severity medium stands; side tool stands.

## F19 [medium] 34 placeable tools require sceneIdentifier while update-token and configure-soundscape default to the active scene
VERDICT: corrected
REASON: Checked: src/tools/placeables/_module.ts:22-25 sceneTarget = z.string().min(1) (required) composed into 24 schemas across drawing/light/note/sound/tile/token/wall + region's own; built the advertised list offline (scratch/perf-schema-lib.mjs over dist/) -- 40 tools carry sceneIdentifier, 38 required, 2 optional (update-token, configure-soundscape). The 5 bare baseline calls each returned the 185-char zod error (bodies/list-tokens.txt etc.). configure-soundscape falls back to game.scenes.active (src/page/soundscape.ts:358). game.scenes.active is one world-wide document (scene.ts:476 activate-scene description). List results already carry sceneId/sceneName (_placeables.ts:158, passed through by placeable-format.ts:68-71). The 4 _placeables.ts resolve sites (:88/:154/:183/:244) are real. Corrections: (1) count -- 32 placeable-family tools require sceneIdentifier (tiles/lights/sounds/drawings/walls x4, list/place/delete-tokens, create-scene-notes/list/update/delete-note, create/list/update/delete-region, add-region-behavior); the other 6 required ones are scene-document tools (update-scene, activate-scene, pull-users-to-scene, set-landing-scene, get-scene-dimensions, screenshot-scene) that should stay required; so 32 (+1) descriptions, not 34/35. (2) update-token does NOT default to the active scene: its description says 'Omit to use the ACTIVE scene' (token.ts:52-56) but src/page/placeables/token.ts:313-315 resolves game.scenes.current, which the repo's own note (scenes.ts:892) documents as 'canvas.ready ? viewed : active' -- the bot's VIEWED scene once the canvas is up, i.e. exactly the guess the finding says to avoid. Only configure-soundscape actually defaults to the active scene today. get-current-scene (scenes.ts:324) also uses game.scenes.current. (3) Page sites: the 4 in _placeables.ts plus add-region-behavior's own resolve (region.ts:627) = 5. Severity medium and side tool are right; the ~20-line estimate holds.
CORRECTION: Title: '32 placeable tools require sceneIdentifier; configure-soundscape defaults to the active scene and update-token claims to but resolves game.scenes.current (the viewed scene).' Fix adds: switch src/page/placeables/token.ts:313-315 from game.scenes.current to game.scenes.active (or correct its description); 5 page sites (_placeables.ts x4 + region.ts:627); 32 descriptions; leave the 6 scene-document tools required.

## F20 [medium] Output formats: 92 string / 51 JSON / 8 mixed with no per-family rule; JSON lists cost 40-55% more than line records; a miss is prose inside a JSON tool
VERDICT: corrected
REASON: Re-ran scratch/perf-results-formats.mjs: raw counts are string 64 / json(body) 64 / string(body) 1 / unknown 22 (=151). The 92/51/8 in the finding is a manual re-classification; I verified it: the 64 json(body) split into 29 non-placeable object bodies + 35 placeable tools, and of those 35 the 27 non-list ones all return strings (formatCreate/Update/DeletePlaceables in src/utils/placeable-format.ts:47/74/92 for x8 create/update/delete, plus string bodies for update-token, update-note, update-region, create-teleporter, add-region-behavior, remap-teleporters) while the 8 list-* return the JSON result or the string 'Scene not found: ...' (placeable-format.ts:68-71) -- so 64+1+27 = 92 strings, 29+22 = 51 JSON (all 22 'unknown' handlers return objects via format*Response helpers -- checked pc.ts:313/396/444/513, npc.ts:273, manage-activity.ts:682, manage-effect.ts:288, content-audit.ts:87, actor.ts:346, actor-creation.ts, update-actor.ts etc.), 8 mixed. Family split confirmed from the .out (scenes/folders/macros/users/playlists/rolltables/cards/chat = string; actors/items/journals/packs/ownership/searches = JSON). Pack-shape mismatch confirmed from the bodies (search-compendium pack:{id,label}; faceted pack:'<id>' + packLabel). Re-ran perf-results-projection.mjs: list-actors 168 records today 13,210 / compact JSON 10,467 / markdown 6,599 / TSV 5,927; list-items 16,392 / compact 7,965 / TSV 4,769 -- identical to the captured .out. Correction is to the overhead claim: with the SAME fields (id,name,type) the comparison is compact JSON 10,467 vs lines 6,599 / 5,927, i.e. line records are 37-43% smaller (JSON is 59-77% larger); the 50-55% figure compares today's body (which also carries hasImage and the envelope) to lines, and list-items 16,392 -> 4,769 is a projection that also drops img/folderId, not a format-only comparison. 'JSON lists cost 40-55% more than line records' is the wrong direction and mixes the two comparisons. The 3.0 rule is on the tool side and rides the already-planned M8 consolidation -- proportionate.
CORRECTION: Evidence sentence: 'for the same fields, line records are 37-43% smaller than JSON (list-actors id,name,type: compact JSON 10,467 vs markdown 6,599 vs TSV 5,927); against today's bodies, which also carry unread fields and an envelope, the saving is 50-55% (13,210 -> 6,599/5,927).' Keep 92/51/8 (verified), the family split, the pack-shape mismatch, and the fix.

## F21 [medium] $defs/$ref is not a lever: reused:'ref' grows the list +18.75%; genuine reuse is 0.34%; cross-tool dedup is not expressible in MCP; the 'shared activity/effect shape' does not exist; the low-level SDK Server is load-bearing
VERDICT: confirmed
REASON: Re-ran scratch/perf-schema-reused.mjs — it CRASHES unless invoked as `node --import ./scratch/perf-schema-hook-register.mjs scratch/perf-schema-reused.mjs` (zodOf is empty without the recording hook; the synthesis should record the invocation). With the hook: tools/list 283,948 -> 337,198 (+53,250, +18.75%), schema part 206,472 -> 259,722, 121/151 tools gain $defs, 1,275 entries; my own count (scratch/skeptic-c5-defs-size.mjs) gives 838 entries <= 60 chars, 213 of them the bare {"type":"string"}. perf-schema-reused-fixed.mjs: genuine intra-tool reuse -966 chars (0.34% of list, 0.47% of schema bytes), 9 tools / 11 defs. perf-schema-dupes.mjs: 28,936 theoretical (10.2%), 12,286 net after $ref overhead (4.3%). grep: 237 `.optional().describe(` sites in src (non-test), 0 `.describe(...).optional()` single- or multi-line. perf-schema-nearDupes.mjs: manage-activity and add-feature share only damageParts.items (+ add-feature's saveDamageParts.items) and healAmount structurally; manage-activity.ts:4-19 imports only enum constants from utils/dnd5e-canonical, no zod object shared with add-feature/grant-to-actor — the 'shared activity/effect shape' indeed does not exist. zod 4.4.3: classic/schemas.js:166-168 describe() clones + registers; core/to-json-schema.js:70-76 re-processes schema._zod.parent (finding cites 68-74, same block), :206-208 extracts when seen.count > 1 under reused:'ref'. SDK 1.29.0: src/index.ts:14,75,80 uses the low-level Server and setRequestHandler(ListToolsRequestSchema, () => ({ tools })); shared/protocol.js:886-893 wraps the handler and :372-388 sends `result` untouched, so $defs pass through; server/mcp.js:78-81 registerTool calls toJsonSchemaCompat with no `target`, and zod-json-schema-compat.js:9-17 maps undefined -> 'draft-7'; verified z.toJSONSchema(..., {target:'draft-7', reused:'ref'}) emits `definitions` — the dialect src/utils/schema.ts's header says bricked a session. HANDOFF.md:79 ('check its reused option') and :132 ('(c) a $ref-factored union') still list this lever as open, so recording the measured negative in the 3.0 plan is the right, proportionate fix (no code change); side 'tool' is acceptable though the fix itself is a plan-doc entry.
CORRECTION: 

## F22 [medium] actorIdentifier is described 16 different ways across 14 tools; sceneIdentifier 15 ways across 43 fields
VERDICT: corrected
REASON: Numbers reproduce (perf-schema-nearDupes.mjs + scratch/skeptic-c5-scene-ids.mjs): actorIdentifier 16 occurrences / 14 tools / 16 distinct texts / 3,122 chars; sceneIdentifier 43 occurrences / 41 tools / 15 distinct / 4,180 chars. Three things are wrong. (1) 'Also accepts a placed TOKEN id' appears in 9 tools, not 11 (remove-from-actor, update-actor, update-actor-item, manage-activity, add-free-cast, manage-effect, apply-condition, add-item, import-item). (2) The sceneIdentifier half does not show 'different contracts': every one of the 15 texts begins 'Scene id or exact name'; page-side there is ONE resolver, resolveSceneStrict (src/page/scenes.ts:1280, id -> exact name) used at 17 sites; and 24 of the 43 fields already share one zod instance (`sceneTarget`, src/tools/placeables/_module.ts:22-25). The 15 variants differ only in a purpose suffix ('holding the placeables', 'to screenshot', 'STRICT — no fuzzy matching', 'Omit to use the ACTIVE scene') — cosmetic, low severity. (3) For actorIdentifier there are TWO deliberate page-side rules, not one 'real resolution rule': fuzzy — resolveActorFuzzy (src/page/_shared.ts:493: id -> exact name -> case-insensitive substring -> token id), used by update-actor, add-feature (features.ts:12 / attacks.ts:16 import it as findActorByIdentifier), level-up-pc (advancement.ts:1055), items, effects, conditions, free-cast, chat, and duplicated verbatim in ownership.ts:22; strict — resolveActorStrict (src/page/assets.ts:268, set-actor-art) and inline id->getName in actors.ts:1295 (delete-actor) and :1430 (duplicate-actor), commented 'STRICT resolution only'. The genuine defects are: add-feature's top-level field says 'Target actor (exact name or ID)' (src/tools/dnd5e/grant-to-actor.ts:26) while it resolves fuzzy + token id; add-feature carries three different texts for the same merged field (grant-to-actor.ts:26, add-feature.ts:87-91, features.ts:17); level-up-pc has no description at all; set-actor-ownership/list-actor-ownership legitimately accept 'all' / 'party characters' and are a different contract.
CORRECTION: Title: 'actorIdentifier is described 16 ways across 14 tools against TWO page-side rules (fuzzy in ~11 tools, strict in set-actor-art/delete-actor/duplicate-actor); add-feature advertises exact-match but resolves fuzzy and carries 3 texts for one field; sceneIdentifier's 15 texts all state the same contract (id or exact name) and 24/43 already share one instance.' Evidence: TOKEN-id sentence in 9 tools, not 11. Fix: TWO shared instances in src/tools/_targets.ts — actorTarget (fuzzy + token id, the rule resolveActorFuzzy implements) and actorTargetStrict (id or exact name, for the destructive/art tools) — plus fold ownership.ts:22's duplicate resolver into resolveActorFuzzy; sceneTarget already exists — extend it with a `.describe()` suffix helper only if wanted. Scope ~14 actor tool files (not ~30); sceneIdentifier work is cosmetic. Severity: medium for the actor half (a wrong contract on add-feature), low for the scene half.

## F23 [medium] Toolsets: actors alone is 42.5% and scenes 33% of the list; the always-on world set is 82% non-lifeline weight
VERDICT: corrected
REASON: All numbers reproduce (perf-schema-toolsets.mjs and scratch/skeptic-c5-world.mjs): actors 39 tools 120,724 (42.5%), scenes 52 / 93,690 (33.0%), actors,compendium 132,633 (46.7%), scenes,assets,audio 110,573, chat,combat 22,659; world per-tool sum 10,636 (10,645 as the array); configure-dnd5e-settings 4,570 + manage-calendar 1,971 + update-user 1,340 + set-user-avatar 816 = 8,697 = 81.8%; the four lifeline tools = 1,939; heavy-four = 38.4% of chat,combat (world as a whole = 47.0%); manage-activity 17,514 + add-feature 13,906 + add-item 11,464 + manage-effect 8,155 + add-region-behavior 5,807 = 56,846 = 20.0%, none CRUD-named. Fix surface checked: src/toolsets.ts (ALWAYS_ON :201, resolveToolsets :214), src/tools/registry.test.ts:481 expects 'world' in the enabled set, README.md:127 and :307 table row — small, config side, proportionate. Two corrections: the world array is src/toolsets.ts:19-28, not :22-31 (22-31 straddles the end of world and the start of actors); and the finding's framing omits the owner's cost model — in Claude Code schemas are deferred, so the always-on set costs 8 names = 315 prefixed chars per registration and a session/settings split saves ~160 chars per prompt; the 8.7k-char saving exists only on eager clients with a narrow FOUNDRY_TOOLSETS (where it is 38-47% of the list, which does justify medium).
CORRECTION: Cite src/toolsets.ts:19-28 (ALWAYS_ON :201). Add to the evidence/title: the saving is eager-client-only on narrow registrations (chat,combat 22,659 -> ~13,962); in Claude Code the split saves ~4 names ≈ 160 chars per prompt. Fix touch-list: toolsets.ts, registry.test.ts:481, README.md:127 + :307.

## F24 [medium] Node<->page seam is name-typed but payload-untyped: 166 call sites, every result any, 26 page handlers take any args
VERDICT: corrected
REASON: Evidence holds: src/foundry.ts:43 `call<T = any>(name: keyof PageApi, args?: unknown): Promise<T>` on the FoundryBridge interface, :447 class impl `call<T = unknown>(name: string, ...)`; src/page/index.ts:367 `PageApi = typeof api`; grep: 166 `foundry.call(` in src/tools non-test (15 more outside src/tools), 0 `.call<` anywhere in src; _shared.ts:85-93 'ZERO return-type protection' (about serializability, but supports the claim). The 26 any-arg handlers reproduce, but arch-seams-pageapi-typing.mjs reports notFound: 42 of 150 — its regex misses const-arrow placeable CRUD and no-arg functions, all typed — so the denominator is 150, not 108 (26 of 150). The fix's scope estimate is wrong. I copied src/ to the scratchpad, changed only the interface to `call<N extends keyof PageApi>(name: N, ...args: Parameters<PageApi[N]>): Promise<Awaited<ReturnType<PageApi[N]>>>` and ran tsc --noEmit: 397 errors in 39 files (scene.ts 40, journal.ts 31, tables.ts 29, asset-bridge.ts 29, group.ts 24, chat.ts 23 …). Decomposition: 282 (90 TS18046 + 192 TS2339 'does not exist on type {}') come from 74 page handlers whose declared return is Promise<unknown> / unknown / Record<string, any> (grep count) — the typed seam hands the tool side `unknown`, so those 74 return types must be written first; 76 arg-side exactOptionalPropertyTypes errors (50 TS2379, 21 TS2322, 2 TS2375, 2 TS2412, 1 TS2739 — `x | undefined` passed into `x?:`); 8 TS2554 `{}` passed to no-arg handlers (cards.ts:139 listCards, ownership.ts:202 getFriendlyNPCs …); 2 TS2345 Record<string, any> into a concrete shape (manage-activity.ts:678, manage-effect.ts:284); ~28 genuine result-shape mismatches (journal.ts:372-473 reads `.error` on results whose declared type never has it). So '~20-40 real mismatches' is right for the genuine class, but 'half a day + fixes' and 'the 26 any handlers as the follow-up' understate: the prerequisite is 74 page return types + 26 any-arg handlers, then ~85 mechanical arg-side edits. Fix direction is right; side 'tool' is right; severity medium holds.
CORRECTION: Evidence: '26 of 150 handlers take any args; 74 handlers declare unknown/any returns'. Fix estimate: flipping the signature yields 397 tsc errors — 282 from the 74 untyped returns (write those first), 76 exactOptionalPropertyTypes arg edits, 8 `{}`-to-no-arg calls, 2 shape casts, ~28 genuine result mismatches (tool reads fields the handler never returns). Order: type the 74 returns and 26 arg shapes page-side, then flip the signature, then fix ~115 tool-side sites. Several days, not half a day.

## F25 [medium] The verify-script corpus (16.5k lines) is the real regression suite while tests/integration (1.3k lines) is the declared one, and the live suite gates on MOLTEN_SERVER_URL
VERDICT: corrected
REASON: Re-ran scratch/arch-seams-scripts-census.mjs: 63 verify-* (16,539 lines by its count; `cat scripts/verify-*.mjs | wc -l` = 16,476 — the finding's 16,512 matches neither, ±0.2%), 60 with finally{}, 63 print counts, release set 5,672 lines (exact, summed per file), tests/integration 8 files / 1,271 lines (wc: 1,263) / 86 it(). tests/integration/setup.ts:46 `HAS_ENV = Boolean(ENV.MOLTEN_SERVER_URL)` and :53-54 warning are exact. Substance holds: setup.ts:66-69 foundryConfig() already derives the bridge config through resolveHostConfig/hostKindFromEnv — the same selector scripts/lib/bridge-config.mjs:16-45 uses — so only the opt-in gate is Molten-shaped and a local-only .env skips every live suite. Fix feasibility checked: vitest.integration.config.ts already sets fileParallelism:false + forks.singleFork (one world driver), testTimeout 180 s, hookTimeout 600 s, and each suite opens its own bridge in beforeAll — promoting the 12 scripts fits the one-driver rule. Two line cites are wrong: docs/RELEASE.md 'needs a live Molten box … MOLTEN_SERVER_URL' is :19-21 (14-16 is the offline-gate code block); scripts/local-foundry.mjs reads `LOCAL_WORLD_ID || MOLTEN_WORLD_ID` at :74 and dies at :172 (:71 dies on LOCAL_FOUNDRY_DATA, :169 is inside the status printer). Proportionality note for a terminal 3.0: the must-do part (gate on the selector's serverUrl, worldId through the selector, RELEASE.md wording) is ~10 lines; the 5,672-line promotion is the optional 3-4 days and should be listed separately.
CORRECTION: Cites: docs/RELEASE.md:19-21; scripts/local-foundry.mjs:74,172; verify-* ≈ 16.5k lines (16,476 by wc). Fix: split into (a) must-do — setup.ts gates on resolveHostConfig(ENV, hostKindFromEnv(process.env)).serverUrl not being the placeholder, local-foundry.mjs takes worldId from the selector, RELEASE.md names FOUNDRY_HOST=local RUN_LIVE=1 npm run test:integration (~10 lines); (b) optional — promote the 12 release-set scripts (5,672 lines) into *.int.test.ts, which the existing vitest.integration.config.ts (singleFork, 600 s hooks) already supports.

## F26 [medium] This repo's .env is the family's secrets file and does not know it (4 undeclared sibling keys); 95 of 100 scripts and 3 siblings hand-roll the same .env loop
VERDICT: corrected
REASON: Checked: `comm` of .env keys vs .env.example → exactly BF_SUITE_USER, BF_SUITE_PASSWORD, MOLTEN_TEST_USER, MOLTEN_TEST_PASSWORD; `git grep` for the four → 0 tracked hits. Sibling line refs are exact: fvtt-mod-battleflow/tools/target.mjs:42 (BF_SUITE_USER), :47 (BF_SUITE_PASSWORD), :64 (MOLTEN_TEST_USER/PASSWORD), the 'deliberately NOT LOCAL_FOUNDRY_USER' comment at :34-41; battleflow tools/harness.mjs:46-54 loadEnv loop; fvtt-mod-fxstudio/tools/lib/foundry.mjs:12-20 loop, :26/:29 keys; fvtt-mod-miscpatches/tools/smoke-teleports.mjs:13-18 loop, :22 key. dotenv is a runtime dependency (package.json:31, src/config.ts:2). The NUMBER is under: `grep -lF` for the exact regex `/^\s*([A-Z0-9_]+)\s*=\s*(.*)\s*$/` gives 95, but 97 of 100 scripts hand-roll the loop — scripts/local-foundry.mjs:52-56 and scripts/pull-prod-to-local.mjs:56-60 carry a `(.*?)` variant of the same loop; the review's own scratch/user-facing-measure.mjs prints 'scripts hand-parsing .env: 97'. The 3 that don't are scripts/lib/bridge-config.mjs (takes `env` as a parameter), measure-tool-results.mjs and verify-toolsets.mjs (spawn the server over stdio, no .env). Fix side (config + a library export) is right and proportionate; but the codemod scope should be sequenced after the F27 cull.
CORRECTION: 97 of 100 scripts hand-roll the .env loop (95 with the identical regex + local-foundry.mjs:52-56 and pull-prod-to-local.mjs:56-60 with a `(.*?)` variant); 3 do not (bridge-config.mjs, measure-tool-results.mjs, verify-toolsets.mjs). The codemod touches 97 files today, or ~58 if it runs after the F27 cull (60 kept minus the 2 stdio scripts) — sequence it after F27.

## F27 [medium] 22 of 99 scripts are house-module harnesses or owner-ops; 17 spikes are historical; a release-set script hard-codes the owner's world; knip never looks at scripts/; deploy-house-module hand-rolls two file planes
VERDICT: corrected
REASON: Reproduced by re-running scratch/user-facing-measure.mjs: 99 scripts (63 verify, 17 spike), split 60 product / 10 house-module / 12 owner-ops / 17 spikes. verify-region-tooling.mjs:25-26 hard-codes `worlds/the-broken-heart-of-greenrest/…webp` and uses it at :51/:60. deploy-house-module.mjs:90-119 hand-rolls the local-fs and WebDavClient planes while src/hosts/types.ts:49-67 already defines FilePlane with write/read/ensureParents. upload-soundscape-library.mjs:31 hard-codes `../fvtt-mod-soundscape-sfx`. knip.json `project` is `src/**/*.ts`. 0 README mentions of the 12 ops scripts. battleflow has tools/reload-clients.mjs (its tools/README.md:150). Old-path references: 12 lines in 7 sibling files reproduce. Six corrections: (1) '4 campaign-bound verifies' is 3 — verify-folder-sort.mjs:68 is a COMMENT citing Greenrest as ground truth; the script tags ZZ-SORT-IT (:24) and touches no campaign document. (2) '8 unreferenced spikes' is 7 by exact filename (spike-advancement-choices, -advancement, -dialogv2-render, -group-currency-dom, -group-render-hooks, -native-scene-import, -placeables-schema); the scratch script's own 'referenced' counter is broken (`require` inside an ESM file → catch → 0 for all 17). (3) 'add scripts/**/*.mjs to knip' must go in `entry`, not `project`: I ran knip offline with scripts in `project` only → 'Unused files (99)', exit 1; with scripts as `entry` (scratch/skeptic6-knip-scripts.json) → clean, exit 0. As entries knip only guards import resolution and unused scripts/lib exports; it can never flag a dead script, so it does not address the spike/one-off problem it is paired with. (4) The 12 old-path lines are all docs (CLAUDE.md, README, NEXT-SESSION.md) and `//` comments (fxstudio tools/sandbox-module.mjs:5,7 and battleflow target.mjs:5 are comments) — 0 executable call sites; a docs break, not a code break. (5) verify-landing-scene.mjs is a hybrid — its header: 'LIVE verification for set-landing-scene + fvtt-mod-openserver's landing-scene routing'; moving it to openserver leaves this repo's set-landing-scene tool with no live acceptance. (6) party-stats.mjs header: 'the dev/archival harness for the get-combat-stats MCP tool' — a product-tool CLI (reads battleflow's stat stamps), not an obvious one-off to delete.
CORRECTION: 3 (not 4) campaign-bound verifies need ZZ-* fixtures: verify-region-tooling.mjs:25-26, verify-token-display.mjs:19, verify-scene-sidecar.mjs:21-22 (verify-folder-sort is already clean; :68 is a comment). 7 (not 8) spikes are unreferenced. Add `scripts/**/*.mjs` to knip's `entry` (with `project` alone knip reports all 99 as unused files); state that this catches broken imports/unused lib exports only, not dead scripts. The sibling old-path references are 12 doc/comment lines, 0 code call sites. Keep verify-landing-scene (or split its set-landing-scene half) in the product set; reconsider deleting party-stats (a get-combat-stats CLI).

## F28 [medium] Skills duplicate ~270 lines of enum/grammar/field paths the schemas already advertise, and it has drifted twice
VERDICT: corrected
REASON: Re-ran scratch/skills-schema-overlap.mjs: 29/31 (manage-effect; absent roll.attack.mode, roll.attack.classification), 21/21, 19/19, 16/16, 10/10, 11/11 reproduce exactly, and the manage-effect schema literally carries the dnd5e.advantage value grammar (`+1 -1 =+1 =-1 >=0 <=0`), the Filter operator list and the expiry enums; add-region-behavior enumerates web/ice/slope and rotateArea cw/timeMs. Both drifts confirmed: stat-block-builder/SKILL.md:157 '`add-feature` takes no `img`' vs src/tools/dnd5e/add-feature.ts:224-231 (img accepted since 91bb350, 2026-06-29 — 11 weeks older than implied) and _shared/authoring-policy.md:50 which already says 'accepts an `img`'; session-audit/SKILL.md:78-79 'does not surface … resistances, or immunities' vs src/tools/dnd5e/actor-stats.ts:203-210 (eeb3b88, 2026-08-14); both skills last edited 2026-09-15. Corrections: the cited ranges sum to 235 lines (123+28+56+28), not ~270, and they are not all enum/grammar — physical-item-builder:66-87 (22 lines) is worked rules recipes (Bracer of Archery / Stone of Good Luck / Flame Tongue shapes) and pc-builder:140-167 is the choices-map worked example plus the `skills:acr…`/wildcard vocabulary that create-pc's schema does NOT carry (5/10 absent), i.e. exactly the content the fix says skills keep. Path cited as `actor-stats.ts` is src/tools/dnd5e/actor-stats.ts. Fix side (skill for the drift, tool for the missing tokens) and ~1 day are right.
CORRECTION: ~235 cited lines, of which the vocabulary-only duplication is roughly 150-185 (stat-block-builder:200-322 grammar/enum bullets, scene-builder:59-70, physical-item-builder:108-130 cast-param block and :145-155 itemType table); physical-item-builder:66-87 and pc-builder:140-167 are recipes/worked examples the fix retains. Missing schema tokens are 2 in manage-effect plus 5 wildcard forms in create-pc (skills:acr, skills:ath, languages:standard, tool:game, weapon:mar). Cite src/tools/dnd5e/actor-stats.ts:203-210 and add-feature.ts:224-231 (img since 91bb350, 2026-06-29).

## F29 [medium] scene-builder's 65-line sidecar section re-derives a hazard create-scene's placeablesPath already removes
VERDICT: confirmed
REASON: Checked every citation: .claude/skills/scene-builder/SKILL.md:82-146 is exactly 65 lines (Step 0.5 header at :82, Step 1 at :148); :104 'Pass the sidecar's `walls` and `lights` arrays straight into `create-scene`'; :107-134 are the two cherry-pick warnings (wall `sight` at :107-119, light `config` at :120-134); `grep -c placeablesPath` on the skill → 0. src/tools/scene.ts:295-303 declares `placeablesPath` ('a JSON file of {walls,lights,regions}'); :550-565 reads it server-side and merges payload.walls/lights/regions into the call; unit tests at src/tools/scene.test.ts:736-770. A Foundry sidecar has top-level `walls`/`lights`, so it fits verbatim, and the legacy→v14 normalization (sense→sight+light, flat light→config) is page-side (src/page/scenes.ts:74-190), so walls fed through placeablesPath still convert even though they bypass the zod SidecarWallSchema parse (merge happens after CreateSceneSchema.parse). tom-cartos-import/SKILL.md:220-226 already routes placeables this way. scripts/verify-scene-sidecar.mjs:74-75 still passes the arrays inline (matches the skill). Fix is skill-side, ~50 lines, and also stops hundreds of wall records transiting the agent. Two small riders: widen the placeablesPath description (scene.ts:298-303 says 'as written by read-pack for a scene-pack import' — 1 line, tool side) so a model reading the schema does not hesitate to pass a sidecar; Step 1 still reads width/height/grid/padding from the sidecar, as the fix already states.
CORRECTION: 

## F30 [medium] set-actor-art leaves inherited prototype settings; two skills re-derive the same 3-param fix-up after every art swap
VERDICT: corrected
REASON: Checked: src/page/assets.ts:131-204 setActorArt builds `update` with only `img` and `prototypeToken.texture.src` (:183-185) — no scale/ring/lockRotation. token-cutout/SKILL.md:38-45 and _shared/authoring-policy.md:75-81 both prescribe update-actor {tokenScale:1, tokenRing:false, tokenAutoRotate:true} after any swap; :46-48 and :82-84 the list-tokens/update-token pass; design.md:130 quote is exact. The evidence 'the create tools already bake these (policy :72-74)' is only 3 of 4: src/page/dnd5e/token-defaults.ts:49-66 bakes lockRotation:false, randomImg:false, ring.enabled:false and is `Object.assign`ed over the source prototypeToken (src/page/actors.ts:1154-1164) keeping `texture` — texture.scaleX/scaleY (2 on 2024-MM actors) is never reset at creation, so policy :71-72 'art scale 1 … the creation tools now bake the corrections' is itself a third drift. That is correct behaviour at creation (MM art is a small subject on a big plate; scale 1 would shrink it) — the scale reset belongs precisely on the texture-swap path, which strengthens the tool-side placement. But the proposed fix 'normalizePrototype applying the same bake as the create path' is wrong twice: the create bake would also rewrite disposition/sight/displayName/displayBars on an art swap and would still miss scale. And lockRotation:false defaulted on contradicts token-cutout:49-52, which gates auto-rotate on a facing check ('art drawn head-down will move backwards') — that check is judgment under §2.1 and should stay in the skill, so auto-rotate must not be an unconditional tool default. Severity, side (tool) and half-a-day stand.
CORRECTION: normalizePrototype (default true) should write exactly {prototypeToken.texture.scaleX/scaleY: 1, prototypeToken.ring.enabled: false}, only when the token texture actually changes (skip on applyToToken:false or a no-op token write), and expose lockRotation as a separate opt-in (`autoRotate?: boolean`, default unset) because the facing check in token-cutout:49-52 is judgment. Do not reuse tokenDefaults() from the create path — it does not touch scale and would rewrite disposition/sight/display fields. Also fix authoring-policy.md:71-72: the creation tools bake auto-rotate/ring/randomImg but NOT scale (and should not).

## F31 [medium] get-actor reports 0 for a PC's max HP; session-audit papers over it with a campaign-repo snapshot
VERDICT: corrected
REASON: Substance holds. dnd5e 6.0.3 source (scratch/dnd5e-6.0.3/module/data/actor/character.mjs:64-67) declares a character's `attributes.hp.max` as `NumberField({nullable:true, initial:null, label:'DND5E.HitPointsOverride'})` — it is an OVERRIDE slot, null on every ordinary PC; the real max is computed only on the prepared document by `AttributesFields.prepareHitPoints` (templates/attributes.mjs:462-468, which also sets `effectiveMax = max + tempmax`). NPC hp.max is stored (npc.mjs:110), so the bug is PC-only. src/page/actors.ts:419 sends `sanitize(toSource(actor).system)` (source), so src/tools/dnd5e/actor-stats.ts:140 `max: hp.max ?? 0` yields 0 in `stats.hitPoints` and actor-stats.ts:27 `max: system.attributes.hp.max` yields null in `basicInfo.hitPoints` — exactly the '0/null' session-audit/SKILL.md:76-77 describes, with the 'propose extending get-actor' at :81. extractDerived (actors.ts:331-387, wired at :491-493) emits abilities/skills/ac/init/legact/xp and no hp. Corrections: (1) line refs — the hp block is actor-stats.ts:135-143 (the `?? 0` at :140), not 136-141; extractDerived spans 331-387, not 331-380; (2) the fix as written is incomplete — adding `hp` to extractDerived alone changes nothing in the get-actor output, because both Node consumers read the source blob: actor-stats.ts:27 (basicInfo) and :140 (stats) must be changed to prefer `actorData.derived?.hp?.max` the same way :33/:146 already do for AC; (3) report `effectiveMax` (or tempmax) too, since dnd5e clamps value against effectiveMax, not max. Severity medium and tool side are right (design.md §2.1: 'never paper over a tool correctness bug inside a skill').
CORRECTION: Evidence lines: actor-stats.ts:135-143 (`max: hp.max ?? 0` at :140) and :27 (`max: system.attributes.hp.max` → null); extractDerived = actors.ts:331-387. Fix: add `hp:{value,max,effectiveMax,temp,tempmax}` from the live actor to extractDerived AND change the two consumers actor-stats.ts:27 and :140 to `actorData.derived?.hp?.max ?? hp.max ?? 0` (mirroring the AC pattern at :33/:146); then drop the party-snapshots fallback at session-audit/SKILL.md:76-81. ~10 lines, not 5.

## F32 [medium] pc-builder re-derives two deterministic rules it says are not judgment: the class->pregen portrait path and prepared:2
VERDICT: corrected
REASON: Substance holds. pc-builder/SKILL.md:226-240 says 'DETERMINISTIC … a fixed 1:1 mapping, not a judgment call', then makes the skill derive `modules/dnd-players-handbook/assets/journal-art/<class>.webp` (:232), the id pattern `phbprg<Class>0000` (:235) and a 3-call confirmation (search-compendium → get-compendium-entry → set-actor-art, :233-236), with the multiclass rule at :237-239. Nothing in src implements it: `grep -rn 'journal-art|phbprg' src/` gives 0 for journal-art and 2 for phbprg, both a test fixture id in src/tools/dnd5e/pc.test.ts:280,283 — not an implementation, but the finding's '0 hits' is not literally true. Always-prepared: _shared/authoring-policy.md:127-133 (rule 14) and pc-builder:196-199 prescribe `update-actor-item {'system.prepared': 2}` per spell; src/page/dnd5e/free-cast.ts:175-176 (`raisedPrepared` → `update['system.prepared'] = 2`) and :250 (`setProperty(data,'system.prepared',2)` on import) already do that write; create-pc's spell path (advancement.ts:917-927 → addSpellsToActor, spells.ts:256-380) imports spells without touching `prepared`. Fix-shape correction: `level-up-pc` has NO `spells` input at all (LevelUpPcSchema, src/tools/dnd5e/pc.ts:97-105 — actorIdentifier/className/choices/hpMode/acceptDefaults only), so 'spells.alwaysPrepared on level-up-pc' is new surface, not a flag on an existing object. Side and proportion are right: the class→art mapping is deterministic (tool), which classes get always-prepared is a house rule (skill).
CORRECTION: 'grep journal-art|phbprg in src/ = 0' → 0 implementations; `phbprg` appears twice as a fixture id in src/tools/dnd5e/pc.test.ts:280,283. Fix: `create-pc` gets `defaultArt` (portrait+token from the primary class's PHB pregen, warn when dnd-players-handbook is absent) and `spells.alwaysPrepared: boolean` (applied in the addSpellsToActor call at advancement.ts:917-927); for level-up-pc, which imports no spells, either add a `spells` block or leave the per-spell `update-actor-item` patch in the skill — do not describe it as a flag on an existing input.

## F33 [medium] Owner/campaign content in skills: session-scribe is the owner's pipeline, soundscape-builder the house module's, tom-cartos-import bakes an OWNER DEFAULT, three audit skills assume an undeclared campaign-repo layout; scene-builder never offers a soundscape
VERDICT: corrected
REASON: Substance confirmed; several cited line numbers are wrong and two counts are regex-dependent. Reproduced with a scratch script (node, chars = String.length): session-scribe/SKILL.md 20,941 chars, soundscape-builder 23,081, tom-cartos-import 21,849, session-audit 15,138, scene-builder 16,838; scene-builder mentions 'soundscape' 0 times (its Step 4 'Offer attachments' at :184-194 offers playlist + journal only). Verified lines: session-scribe :61 `fvtt-campaign-greenrest`, :109 `msedge.exe` path, :114 `Desktop\<Campaign> Session N`, :143 'recap.html house style (owner-locked 2026-07-08…)' with PC names (Morgash :199,:206) and dated owner-feedback stamps; scripts/setup.ps1:2-3,18-24 winget/CUDA (the path is session-scribe/scripts/setup.ps1). soundscape-builder :307-320 'author soundscapes against prod', :341-342 remap script, :12/:80 the 400-template library. tom-cartos-import :35-44 'OWNER DEFAULT — MAPS ONLY … Ostenwold → Greenrest', :41-42 autoexplore stamp, :24/:65 Molten. session-audit :45 'Morgash takes the maul', :59/:77/:156 `plot/` `sessions/` `party-snapshots/`. plot-drift-check :27-31 (campaign pointer memory at :28). bestiary-builder :38 campaign-repo `sessions/<date>/recap.md`. Artificer illustration-builder/SKILL.md 'molten5e' at :20, :25, :244. Wrong: session-audit 'ultracode' is at :31 and :37, not :27 (:27 is 'Numbers before opinions'); the session-audit owner rule is at :220 and :222, not :170 (:170 is the generic 'Ordering' bullet); plot-drift-check :18 is a generic drift sentence, not an owner/campaign line. The '57 owner/campaign hits' and '19 directive stamps' do not reproduce as stated — a plausible regex (greenrest|campaign|owner|craig|msedge|desktop|molten|house|partystash) gives 66 tokens on 51 lines, and dated parenthetical stamps count 12 (26 lines mention owner/house rule/a date); the counts depend on the pattern and were not given one. Skill side and ~1 day are proportionate.
CORRECTION: session-audit ultracode at :31,:37 (not :27); session-audit owner rule at :220,:222 (not :170); drop plot-drift-check :18 (keep :27-31); artificer 'molten5e' at :20,:25,:244; setup.ps1 lives at session-scribe/scripts/setup.ps1; replace '57 owner/campaign hits' and '19 directive stamps' with a stated regex or with '~50 owner/campaign lines, ~12 dated owner stamps'.

## F34 [medium] Phase-2 language in design.md (18 lines), README, four docs, five skills and one tool description; journal-builder's '§8 landing zone' section is stale twice over
VERDICT: corrected
REASON: Inventory reproduces almost exactly. `git grep -n -i -E 'phase[ -]?2|wait-for-events|event bridge|session-assist|live assist|NUC|companion module|half 2'` on HEAD: design.md 18 hits (25,26,151,164,165,170,172,190,309,315,317,319,320,337,345,348,355,362); scene-placeables-architecture.md 10 (49,98,116,328,349,447,505,548,551,574); architecture-review.md 4 (50,76,100,202); alignment-plan.md 4 (20,41,123,205); plan-2.1:118; compendium-lookup-plan:54; cast-spells.ts:218; verify-actor-tooling:346; verify-write-tools/-dnd5e-writes/-journal-tooling:1; HANDOFF.md and CLAUDE.md 0. The design.md section ranges (18,21-22,25-28; §4 rows 151-155; legend 161-162; 164-173; §5 190-191; §8 302-366 before §9 at 367) are the rewrite scope and check out. README:202-204 ('is the next phase (see design.md §8), not built yet') is real but is NOT caught by the brief's grep regex — worth saying so the scrub does not rely on that grep alone. src/tools/journal.ts:282 is inside the update-quest-journal tool description ('the §8 session-log/progress path') and :437 a docblock; journal-builder :18,:48,:87,:140-148 ('§8 landing zone … Build that now by hand' — stale because §8 goes AND session-scribe already files the Session Diary); _shared/authoring-policy.md:33-34 'a future phase'; cards-builder :10-11 (description) and :89-92; playlist-builder :113-117; stat-block-builder :66-67 'the future PC-actor builder' while create-pc exists (pc.ts:209). Two corrections: (1) the verify-wake.mjs homonym is at :6 and :118-119 on HEAD, not :3,:91-92 — the cited numbers predate commit 96ed726 which added ~27 lines; (2) 'five skills' is four SKILL.md files plus _shared/authoring-policy.md. Docs side and the replacement shape are proportionate for a terminal 3.0; 'historical docs archived not edited' is consistent with F35.
CORRECTION: scripts/verify-wake.mjs homonym lines are :6 and :118-119 (not :3,:91-92); 'five skills' = journal-builder, cards-builder, playlist-builder, stat-block-builder + _shared/authoring-policy.md; note that README:202-204 is not matched by the brief's Phase-2 grep regex ('next phase (see design.md §8)'), so the scrub grep must add 'next phase|§8'.

## F35 [medium] README is 26% release narrative and Molten-first; CLAUDE.md/LOCAL.md are gitignored; 10 of 15 docs are historical; no CHANGELOG
VERDICT: corrected
REASON: Mostly reproduces, with one refuted sub-claim and several numbers off. Checked: README.md is 449 lines (`wc -l`, file ends with a newline), not 450; release narratives 27-141 = 115 lines = 25.6% ≈ 26% ✓; '## Requirements' at :231 ✓; 'molten' 35× in README ✓ and 29× in .env.example ✓; README:238 'hosted on Molten' ✓ but 'to Foundry on Molten' is at :405, not :406. .gitignore:2-3 ignores CLAUDE.md and LOCAL.md ✓ (git ls-files confirms neither is tracked; HANDOFF.md and docs/RELEASE.md are). REFUTED sub-claim: 'CLAUDE.md holds the only copy of the release verify list' — the identical `verify-{effects-6,…,region-tooling}` set plus 'ONE driver at a time' is in tracked HANDOFF.md:100-102 (and :29); the true gap is that docs/RELEASE.md (the declared gate) names only the integration suite + a smoke and never the 12-script set, and HANDOFF is slated for retirement (HANDOFF.md:3-5), so the list would become untracked-only after that. CLAUDE.md line refs are also wrong for the file on disk (50 lines): the release set is at :34-36 and the live-proof discipline at :43-50, not :28-33 / :38-43. docs/: 10 files ✓, 2,485 lines (`wc -l`), not 2,465; status headers show 8 done trackers/reviews (alignment-plan 'Phase-1, NPC-era' tracker; architecture-review self-declared 'SUPERSEDED SNAPSHOT (2026-06-24)'; compendium-lookup-plan; tom-cartos-import-plan 'DESIGN COMPLETE'; scene-placeables-architecture; dnd5e-6.0-compat-review; plan-2.1; plan-2.2) while RELEASE.md and local-sandbox.md are live — so the title's '10 of 15 docs are historical' and the fix's 'the 10 trackers/reviews → docs/history/' both overcount: it is 8 of the 10 docs/ files (9 of 15 counting HANDOFF). No CHANGELOG.md ✓ (tags v1.5.2…v2.2.0, and RELEASE.md:44 says 'update any changelog' with none to update). Docs side is right; the 7-file doc set is large but the owner already deferred the README rewrite to 3.0 (brief item 6), so it is proportionate.
CORRECTION: README 449 lines (115/449 = 25.6%); 'to Foundry on Molten' at README:405; docs/ = 2,485 lines; 8 (not 10) of the docs/ files are done trackers/reviews — move those 8 to docs/history/, rewrite RELEASE.md and local-sandbox.md in place; '10 of 15 historical' → '8 of 10 docs/ files (+ HANDOFF)'; drop 'CLAUDE.md holds the only copy of the release verify list' — HANDOFF.md:100-102 (tracked) carries it today; state instead that docs/RELEASE.md, the declared gate, omits the 12-script release set and the one-driver rule, and that both live only in CLAUDE.md once HANDOFF retires; CLAUDE.md refs are :34-36 (release set) and :43-50 (live-proof discipline).

## F36 [medium] No premium-book presence check; get-world-info never says which books are installed; design.md §2.3 lists 3 prefixes, the code 5
VERDICT: corrected
REASON: Checked: src/utils/compendium-sources.ts — PREMIUM_BOOK_PREFIXES is at :40-48 (the five strings at :42-47: MM, PHB, DMG, dnd-heroes-faerun, dnd-ravenloft-horrors-within); packPriority is at :128-132, NOT :143-147 as cited; isPremiumBookPack is only used by advancement.ts/manage-activity.ts, while both search paths (src/page/compendium.ts:97 and compendium-facets.ts:391) use excludeSrdPacks + packPriority, so a missing PHB silently falls through to any non-SRD pack — confirmed. src/page/world.ts:55-82 getWorldInfo returns id/title/description/background/system/versions/users/automation and src/tools/scene.ts:935-960 formatWorldResponse adds host+user counts — no packs, confirmed. spells.ts:321-323 and compendium-features.ts:87 throw 'No valid compendium packs available' when the named pack is absent — confirmed. design.md:63-64 and :236-237 list three prefixes — confirmed. README.md:231-239 Requirements name Node, Chromium, Foundry 14.368+, dnd5e 6.0.3+, Molten, a passwordless user — no premium module, confirmed. scratch/research-dnd5e-6.0.3.diff:9,21 adds dnd-arcana-unleashed and dnd-deadfall to official-content — confirmed. Fix side (tool: a deterministic presence check in get-world-info) is right and ~25 lines is plausible.
CORRECTION: Line cite: packPriority tier 1 is compendium-sources.ts:128-132 (PREMIUM_BOOK_PREFIXES :40-48). Fix wording: README should not call all five packs a hard requirement — design.md:64 says only MM/PHB/DMG are 'assumed always installed'; heroes-faerun and ravenloft are optional extensions, and the non-authoring tools work without any of them. State MM/PHB/DMG as required for authoring (module ids dnd-monster-manual, dnd-players-handbook, dnd-dungeon-masters-guide) and the other two as optional; report all five (present|missing) in get-world-info.

## F37 [medium] local-foundry.mjs and docs/local-sandbox.md assume the owner's Windows install and the prod->local mirror; .env.example:93-94 is mangled
VERDICT: corrected
REASON: Checked scripts/local-foundry.mjs at HEAD: the bare readFileSync('.env') is at :52 (top-level, before the try/catch at the bottom, so ENOENT is uncaught — confirmed); 'LOCAL_FOUNDRY_DATA missing' die() at :71; the Windows default 'C:\\Program Files\\Foundry Virtual Tabletop\\resources\\app\\main.js' at :76-77; LOCAL_ADMIN_KEY is enforced at :154 ('launch/stop are admin-gated'), the :30-33 cite is only the header comment. The cited :46/:63/:69-70 match neither HEAD nor 3839bbe (:49/:68/:73-74) — the header grew in 4bc157b/96ed726. docs/local-sandbox.md:119-120 'prod is v14; local is v14.365' is stale (sandbox 14.368 per CLAUDE.md/README:30) — confirmed; 'First-time machine setup' is :124-129 with step 4 = pull-prod-to-local at :129, not :130-135; the doc's frame is the prod→local mirror (:11 table, :51, :189). .env.example:93-94 via cat -A: 'resourcesappmain.js' and 'C:Program FilesFoundry Virtual Tabletop^Mesourcesappmain.js' — confirmed, and :94 contains a literal carriage-return byte (the \r of \resources was interpreted), not just eaten backslashes. README.md never mentions local-foundry.mjs or local-sandbox.md (0 hits), so the local host's launcher is reachable only via docs/. Fix side scripts/docs is right; ~30 lines + doc split is proportionate.
CORRECTION: Lines at HEAD: local-foundry.mjs :52 (readFileSync .env), :71 (LOCAL_FOUNDRY_DATA die), :76-77 (default app path), :154 (LOCAL_ADMIN_KEY enforcement); docs/local-sandbox.md :120 (stale v14.365) and :124-129 (setup, step 4 at :129). .env.example:94 also carries an embedded CR byte — rewrite both lines, do not just re-insert backslashes.

## F38 [medium] send-chat-message advertises the plane name 'webdav' in its schema and descriptions
VERDICT: corrected
REASON: Checked src/tools/chat.ts: :40 z.enum(['upload','webdav','dataUri']), :43 alias text, :91 'uploaded to the world over WebDAV', :276 'upload over WebDAV' — all confirmed; plus :23,:26,:357-358 comments and :365 the TS union. Upload goes through this.host.files (:400-414, :510-520): src/hosts/local-files.ts is node:fs (:1-19), src/hosts/generic.ts:28 files = null → refusal — confirmed. git grep -i 'webdav|molten|magic.?url' in src outside hosts/tests, non-comment lines → only chat.ts:40,43,91,276,365, so 'only host-shaped token in an advertised contract' holds. Callers: the only place anywhere in the family that passes embed:'webdav' is this repo's .claude/skills/chat-and-narration/SKILL.md:53,58 (0 hits in artificer/battleflow/fxstudio/miscpatches/soundscape-sfx/campaign), and that skill wrongly calls webdav 'the default' (chat.ts:41 defaults to 'upload') and names MOLTEN_WEBDAV_PASSWORD at :83-84. The FIX MECHANISM is wrong: reproduced with scratch/skeptic8-f38-embed-enum.mjs under toInputSchema's options (io:'input', zod 4): z.enum([...3]).transform(...) still advertises enum ['upload','webdav','dataUri']; z.preprocess(v=>v==='webdav'?'upload':v, z.enum(['upload','dataUri'])) advertises ['upload','dataUri'] and still parses 'webdav' → 'upload'.
CORRECTION: Do not use .transform — in zod 4 input-mode JSON schema it keeps 'webdav' in the advertised enum. Either z.preprocess(...) over z.enum(['upload','dataUri']) if an alias is wanted, or (proportionate for a major, since the only caller is the in-repo skill) drop 'webdav' outright and edit chat-and-narration/SKILL.md:53,58 (embed 'upload' is the default) and :83-84 (MOLTEN_WEBDAV_PASSWORD → host file plane) in the same commit; also fix chat.ts:23,26,357-358,365.

## F39 [medium] Cross-repo contracts are prose-only and asymmetric: the artificer names 11 tool names (5 in CRUD families), three reverse flag contracts are documented only on the sibling side, and the 3.0 rename checklist spans 6 repos
VERDICT: corrected
REASON: Re-ran scratch/siblings-toolnames-in-siblings.mjs: artificer 8 files / 13 names, illustration-builder/SKILL.md exactly the 11 listed; battleflow 6 files / 5 names; fxstudio 2; miscpatches 1; fvtt-mod-soundscape 1 (configure-soundscape); campaign 15 files / 21 names — all reproduce (lootshelf HANDOFF.md 4 names is an unlisted extra). battleflow tools/target.mjs 'disconnect-bridge' is at :91 not :92; ARCHITECTURE.md:300-330 'The data plane — stat stamps' under §4 with the :307 'reader is SHIPPED' warning — confirmed. This side: src/page/combat-stats.ts:4-17, soundscape.ts:3-27, users.ts:41, scenes.ts:924 — confirmed. artificer README.md:98 links Txpple/fvtt-mcp-molten5e, SKILL.md:25,244 'molten5e', CLAUDE.md:27-29 'never talks to the Foundry bridge' — confirmed; artificer src/utils/schema.ts diff vs ours ignoring comment lines → identical. Counted the rename surface: git grep -l molten5e → battleflow 27, fxstudio 15, campaign 11, artificer 10, miscpatches 2, soundscape-sfx 1, soundscape 1 files; 0 files anywhere name fvtt-mcp-dnd5e. Two things off: (a) the artificer rename is 10 tracked files, not 6 (SKILL.md, CLAUDE.md, README.md, ROADMAP.md, notes/m0-spike-2026-09-19.md, notes/archive ×2, plus comments in src/registry.ts, src/utils/schema.ts, scripts/token_cutout.py); (b) 'documented only on the sibling side' is overstated — the three reverse contracts are documented on this side as source headers (combat-stats.ts:1-17, soundscape.ts:1-21, users.ts:38-40) and README.md:348-359 names the modules; what is missing is a docs/ page, which the fix supplies. Fix (docs/contracts.md, stable names, sibling commits in the rename move) is on the docs/sibling side and proportionate.
CORRECTION: Artificer rename = 10 tracked files (7 docs/skill/notes + 3 code comments), not 6; battleflow target.mjs:91; the reverse contracts ARE documented here as file headers — the asymmetry is 'no docs/ page + no version pin', not 'undocumented'. The family checklist is 6 repos / 56 files; the owner's campaign repo adds 11 more.

## F40 [medium] dnd5e 6.0.2/6.0.3 need three targeted live checks beyond the release set; the release notes under-report six behaviour changes; prod cannot take 6.x before Foundry >= 14.367
VERDICT: corrected
REASON: Evidence checked and holds: scratch/research-dnd5e-compare.txt 15 commits/19 files and 7/9 (diff files counted 19 and 9); #7450 sourceItem at 6.0.2.diff:305-333,497; #7482/#7484 at 6.0.3.diff:69-78 — 'turnStart' now only when Number.isFinite(value) in a started combat; cac30cc #7470 at 6.0.2.diff:457-461 drops the isView and viewedScene===scene requirements; system.json compatibility.minimum 14.367 (scratch/dnd5e-6.0.3/system.json:7); foundryvtt.com/releases fetched today: 14.368 (2026-09-16) is the newest stable. Elevation writers are src/page/placeables/token.ts:75,238,346 (the finding omits the path; src/page/token.ts does not exist); grep -l elevation scripts/verify-*.mjs → only verify-reskin-visibility; verify-effects-6.mjs:488-509 and §5b (:591-670) test an EXPLICIT bare expiry, never a no-duration effect on a combatant — confirmed. BUT the finding is stale against HEAD: commit c53c2de (2026-09-20 12:57, 'dnd5e 6.0.3 compatibility pin') already ran the pin step — scratch/pin-6.0.3-targeted.mjs 4/4 (#7482 no-duration effect in combat via core and via manage-effect keeps expiry null; #7450 sourceItem.type condition persisted verbatim), the 12-script release set, integration 85 passed / 1 skipped, and moved the pins: README.md:30 and :237 now say 6.0.3, :68 says 're-checked against the 6.0.3 diff' (no line says 6.0.1 as a pin), LOCAL.md:36 6.0.3, plan Progress :171-183. Still open: (3) the #7470 elevation write on an unviewed Levels scene with disableFalling=false (only 'noted' in the commit), the sourceItem.* vocabulary in manage-effect docs (0 hits in src/tools, src/page, skills), and check (2)'s semantic assertion (item = rolled vs sourceItem = effect's item) — the pin proved persistence only. Count nuance: 6 unnumbered standalone commits (51f6e8f, b642ce0, 3a922ac, 8c10c1a, 0fd354f, 160e7ff; 7b0b142 is the #7459 follow-up) plus #7470, numbered but absent from the release body — seven changes the notes omit.
CORRECTION: Mark F40 mostly DONE at c53c2de: pins moved, release set + integration green, targeted (1) done, (2) done as persistence-only. Remaining for 3.0: the #7470 elevation/falling live check (writers src/page/placeables/token.ts:75,238,346; restore in finally), add sourceItem.* to manage-effect.ts:37-38,85-89,141-142 prose, optionally a roll-based assertion for (2). Severity → low; drop 'README.md:30,68,237 say 6.0.1' and 'move the pins'.

## F41 [low] list-macros: 8,633 chars for 105 macros, no skill calls it, no filters, description promises an icon + command preview the output lacks
VERDICT: corrected
REASON: Checked src/tools/macros.ts: ListMacrosSchema = z.object({}) at :73; the list-macros description at :112-114 promises 'icon, a command preview, and every user hotbar slot'; handleListMacros :141-153 prints only name/id/type/author/hotbar (:151). grep list-macros .claude/skills -> 0 hits (only scripts/measure-tool-results.mjs and scripts/verify-macro-tooling.mjs reference it). Re-measured scratch/bodies-2026-09-20/list-macros.txt: 8,633 chars (9,021 bytes), 105 '- **' lines, 82.2 chars/macro; measure-2026-09-20c.json row = 8633 chars / 134 ms. Re-ran scratch/perf-results-projection.mjs: list-macros 8633 -> 7639 (12%) and 'no hotbar' 8633 -> 6496 (25%) reproduce. Two things are off: (1) the 6,496 / 25% variant is not hotbar detail alone — perf-results-projection.mjs:92 also rewrites ' · by ' to ' · '; dropping the hotbar text only gives 8,633 -> 6,811 (−21%); the pin-count variant 7,639 (−12%) is right. 69 of 105 lines carry hotbar pins. (2) The page side already returns img and commandPreview (src/page/macros.ts:34-44 describeMacro) — the description/output mismatch is purely the tool formatter dropping them at macros.ts:151, so 'print the promised preview' is a one-line formatter change, no page-side work. Fix (filters + includeHotbar) and side=tool are right; severity low is right.
CORRECTION: Hotbar-only share is 12–21% (7,639 with a pin count; 6,811 with the hotbar text dropped); the 6,496 / 25% figure also abbreviates ' · by '. The icon + command preview are already in the page payload (src/page/macros.ts:41-42) — the fix is to print m.img / m.commandPreview in the formatter at src/tools/macros.ts:151 (or drop them from the description at :112-113).

## F42 [low] First call carries the bridge connect (8.8 s): /join is loaded twice; the phase split is unmeasured
VERDICT: confirmed
REASON: Checked src/foundry.ts: doConnect :140-158 (chromium.launch :146, wake :151, ensureWorldReady :152, joinWorld :153, injectBundle :154) has no phase timestamps — the only log is the final info at :157. probeJoinState :241-268 does page.goto /join at :243 and polls locator(JOIN_USER_FIELD).count() at :255-259 until the form is present. joinWorld :347 then does page.goto /join again unconditionally at :357 and waitForSelector on the same JOIN_USER_FIELD at :359, so the second navigation repeats a load the probe already completed. waitForFunction(game.ready) at :410-411; injectBundle addScriptTag at :435-436; dist/page.bundle.js is 532,338 bytes and esbuild.page.mjs has no minify option (unminified confirmed); the bundle is read once at construction (:123) and injected per connect. connect() is lazy — call() awaits this.connect() at :512/:539 — so the first tool call carries it; measure-2026-09-20c.json get-world-info row = 8,840 ms with no second get-world-info in the run (the '124-167 ms later' figure is the range of two other trivial reads, list-users 124 / get-current-scene 167; list-cards was 119 — a nit, not an error). The active scene in bodies/get-current-scene.txt is 'Party Camp' 3080x2380. screenshot-scene is a Playwright page.screenshot of the rendered page (foundry.ts:516-523), so core.noCanvas would break it — the 'leave game.ready alone' part is right. The fix — skip the goto at :357 when locator(JOIN_USER_FIELD).count() > 0 (exactly what the probe just confirmed; ensureWorldReady only returns 'joinable' with the page sitting on the form) — is a two-line change and safe; adding phase timestamps before touching anything else is proportionate for a terminal 3.0. Could not verify offline: that headless Chromium is on SwiftShader and what the canvas draw costs — the finding itself says the split is unmeasured.
CORRECTION: 

## F43 [low] list-actors and list-journals have no name filter, so a name->id lookup pulls the whole list (13,210 / 9,660 chars)
VERDICT: corrected
REASON: Lines and numbers hold: ListActorsSchema src/tools/actor.ts:61-66 is type-only; ListJournalsSchema journal.ts:90-108 has filterQuests/includeContent/journalId/pageId and no name field; list-items has nameFilter (items.ts:52) and list-scenes has filter (scene.ts:341 — the finding's ':345' is inside the same block); page-side listActors at src/page/actors.ts:287 and listJournals at src/page/journals.ts:67 (no args); measure-2026-09-20c.json: list-actors 13,210 / list-journals 9,660. The handlers already forward args (actor.ts:301 passes {type}; journal.ts ~:543 passes {} to listJournals), so '3 page-side lines + a schema field' each is about right (+ one arg pass-through). What does not hold is the skill-usage justification: plot-drift-check uses list-actors 'for names' as a whole-world survey (SKILL.md:52) — a name filter cannot shrink an audit that wants every name; token-cutout says 'find the actor with list-actors / get-actor' (SKILL.md:31) but get-actor (actor.ts:20-25 'Character name or ID') and set-actor-art (asset-bridge.ts:37 'Actor id or exact name') take the name directly, so no list is needed, and its folder mention (SKILL.md:34-36) is export-actor disambiguating same-named actors, not a request for folder on list-actors; bestiary-builder resolves the Bestiary by game.journal.getName in its own script (bestiary-entry.mjs:199) and calls list-journals only with journalId for the post-verify read (SKILL.md:110); journal-builder already lists search-journals, whose searchType:'title' (journal.ts:113-121) is an existing name->id path for journals. So none of the four skills is shown doing a list-then-pick that nameFilter would shrink; the beneficiary is an ad-hoc model lookup. Adding nameFilter is still cheap and right for symmetry with list-items/list-scenes; severity low stays.
CORRECTION: Evidence should read: no skill is shown doing a list-then-pick name->id — actor tools accept names directly and search-journals searchType:'title' already covers journals; the gain is for ad-hoc lookups and symmetry with list-items (items.ts:52) / list-scenes (scene.ts:341). Drop 'token-cutout also wants folder on list-actors' (SKILL.md:34-36 is about export-actor). Fix as written (nameFilter on both, page/actors.ts:287 and page/journals.ts:67 + schema field + arg pass-through at actor.ts:301 / journal.ts listJournals call); folder on list-actors is optional, not skill-driven.

## F44 [low] Registry derivation is one-directional: a definition without a handler is silently dropped (one orphan exists) and every name is spelled three times
VERDICT: corrected
REASON: Re-ran scratch/arch-seams-registry-triplication.mjs: 'definitions collected: 152 | handlers: 151 | toolset entries: 151', orphan ['add-features-from-compendium'], and 3 files (class, registry.ts, toolsets.ts) for all 5 sampled names — the counts reproduce. But the mechanism is misdescribed: the 152 is the script's own tally from instantiating every class registry.ts imports; registry.ts:159-201 never spreads dnd5eFeaturesFromCompendiumTools.getToolDefinitions() into defByName (the class is instantiated at :108-112 only for handleAddFeaturesFromCompendium, dispatched by the composite add-feature handler at :279). My scratch check of the defByName block: 36 classes + addFeatureTool spread; instantiated-but-not-spread = [dnd5eAddFeatureTool, dnd5eFeaturesFromCompendiumTools]. So defByName holds 151 today, the orphan is never 'silently dropped by the derivation' — it is a dead getToolDefinitions() at src/tools/dnd5e/features.ts:62-98 called only by features.test.ts:52-55 — and the proposed guard defByName.size === Object.keys(handlers).length would pass today without deleting anything, i.e. would not catch this orphan. Same pattern: DnD5eAddFeatureTool.getToolDefinitions() (add-feature.ts:554-557) is also never spread (buildAddFeatureTool at :157 builds the advertised composite) — a shadow definition the script does not flag because 'add-feature' has a handler. Line cite is off: the derivation (Object.keys(handlers).filter(isEnabled).map(defByName.get)) is at registry.ts:466-472; :449-457 is the toolset check. toolsetOf() (toolsets.ts:204-210) does overwrite duplicates, but registry.test.ts:450-465 ('every dispatchable tool is in exactly one toolset') already guards that offline — not an open gap. What holds: the derivation is one-directional (an extra defByName entry would go unnoticed); each name is spelled in 3 files; registry.test.ts:320-329 is tautological (tools are derived from handlers, so both filters are empty by construction). Severity low and side=tool are right; the one-record-per-module collapse riding the consolidation is proportionate.
CORRECTION: Evidence: the orphan is an uncollected dead getToolDefinitions() (features.ts:62-98; registry.ts:159-201 never spreads it), not a definition dropped by the derivation at registry.ts:466-472; defByName is 151 today. Fix: delete the dead getToolDefinitions() on DnD5eFeaturesFromCompendiumTools (and its test features.test.ts:52-55), likewise the never-advertised DnD5eAddFeatureTool.getToolDefinitions() at add-feature.ts:554; the defByName.size === handlers.length assertion is still worth adding for the structural case but does not detect these two. Drop the toolsetOf() duplicate point (guarded by registry.test.ts:450-465).

## F45 [low] src/page/actors.ts is the one monolith worth splitting in 3.0: 2,243 lines, 14 exports, 5 offline tests covering 1 export
VERDICT: corrected
REASON: Reproduced: wc src/page/actors.ts = 2,243; git show 6bbf6be (2026-06-24, the June review commit):src/page/actors.ts = 1,750 lines (+28.2%); 14 'export' lines at exactly the cited positions (:287,:401,:508,:548,:650,:802,:1013,:1048,:1280,:1377,:1489,:1557,:1644,:2172); actors.test.ts has 5 it() all under describe('extractDamageProfile') and no other test file imports from actors.js; scenes.ts 1,465 lines / 24 exports / 42 it(); advancement.ts 1,374 / 21 / 25; re-ran scratch/arch-seams-test-shape.mjs: 'src/page modules: 53 (22269 lines); with NO sibling .test.ts: 18 (5076 lines = 23%)'. Three corrections: (1) '62 (globalThis as any).X refs in src/page' — 62 is every globalThis token in non-test src/page (including 'globalThis as { game?… }' and typeof forms); the '(globalThis as any).X' form is 50 (64 with tests), and only 3 are in actors.ts (:50 module-scope ActorClass capture, :1391 lazy re-read for test stubbing). (2) The 18 untested modules include dnd5e/foundry-doc-mock.ts (a test helper, 332 lines) and index.ts (the bundle entry, 375 lines), so 16 real modules. (3) The June review already demoted this exact 5-way page/actors.ts carve to P4 and cut it as a prerequisite (docs/architecture-review.md:196, :216, :221); the finding does not say so. For a terminal 3.0 in maintenance mode a ~2-day zero-behaviour split is only justified because the actor-family consolidation commit reopens the file — the fix's 'in the same 3.0 commit' framing is the right one and should be stated as the sole reason. Side=tool, severity low, and 'leave scenes.ts / advancement.ts' are right.
CORRECTION: '62 globalThis refs' -> 50 '(globalThis as any).X' refs in non-test src/page (62 counts every globalThis token; 3 of the 50 are in actors.ts). '18 of 53 untested' -> 16 real modules once foundry-doc-mock.ts and index.ts are excluded. Add: the June review demoted this split to P4 (docs/architecture-review.md:196,216,221); it returns only as a rider on the actor-family consolidation commit, not as standalone 3.0 work.

## F46 [low] Host.worldId is config the page already knows post-join; only auto-launch needs it and it could be discovered; the wake is generic in mechanism
VERDICT: confirmed
REASON: Checked every cite. src/foundry.ts:202 gates launch on `this.cfg.adminKey && this.cfg.worldId`; :275/:286/:299/:307 are all inside the same launch path (one consumer). src/tools/chat.ts:335 builds `worlds/${this.host.worldId ?? 'world'}/assets/chat` — the bogus `worlds/world/` fallback is real, and src/hosts/types.ts:80 documents worldId's purpose as exactly that default path. src/page/world.ts:68 returns `id: game.world.id`. src/hosts/molten.ts:61-67 `wake()` is one `ctx.goto(this.cfg.magicUrl)`; :75-79 `redact()` strips the magic URL and `?s=` tokens; src/hosts/generic.ts has no wake and `redact` is identity. `fileBrowserUrl` has 0 reads: only env.ts:66 (schema), env.ts:119-120 (assign from MOLTEN_FILEBROWSER_URL) and molten.ts:28 (interface) — dead config, confirmed. No world-id cache exists in the bridge today (grep `game.world|worldInfo` in src/foundry.ts → 0), so the 'cached getWorldInfo' is new code, but small. Two fix details, not refutations: (a) foundry.ts:286 waits for ONE tile by id, so sole-world discovery is new enumerate code and must scope the selector to world tiles (systems/modules also render as `li[data-package-id]` on /setup — not verifiable read-only); (b) dropping MOLTEN_FILEBROWSER_URL also touches .env.example:47 and README.md:325. Side (tool/config) and half-a-day riding F1 are proportionate.
CORRECTION: 

## F47 [low] Owner's box name 'eoh-test' and 'greenrest' survive in src comments and test fixtures despite LOCAL.md saying they were scrubbed
VERDICT: confirmed
REASON: Re-ran the grep. `git grep -n -i eoh-test` (scratch excluded) → exactly the 11 cited lines: src/foundry.ts:63, src/hosts/webdav.ts:20, src/hosts/webdav.test.ts:30,107,119,125,132, src/tools/assets/index.test.ts:59,143,147,470. `git grep -n -i greenrest -- src` → exactly the 6 cited lines: src/hosts/env.test.ts:16,65,97,151 (MOLTEN_WORLD_ID / worldId fixture), src/tools/actor.test.ts:104 (`world: 'greenrest'`), src/page/dnd5e/content-audit.test.ts:68 ('villain in Greenrest'). LOCAL.md:3-4 says these values 'were scrubbed out of the public repo' — the contradiction holds. 17 lines total, so '~20-line sed' is right; the fixtures are opaque strings (webdav tests only need the http→https scheme kept at :119/:132), no behaviour change. Severity low and side tool (src comments + fixtures) are right.
CORRECTION: 

## F48 [low] Stale dist/tools/molten/ build output and a 'tools/molten' comment survive the rename
VERDICT: corrected
REASON: Evidence reproduces: dist/tools/molten/{dav-access,index,webdav}.js (+ .d.ts and .js.map, 9 files) dated 2026-09-19 19:14; src/tools/molten/ absent; package.json build = `tsc && node esbuild.page.mjs` with no clean; `grep -rn tools/molten src scripts tests` → only src/tools/asset-bridge.ts:8; dist/ ignored at .gitignore:32; keyword `molten-hosting` present; the only other src-less file in dist is page.bundle.js (esbuild output, legit). But the fix is half wrong: `tsc --build --clean` does NOT remove orphaned outputs — I built a temp project (src/a.ts + src/ghost.ts), deleted ghost.ts, ran `tsc --build --clean`, and ghost.js survived (tsc only deletes outputs derivable from current inputs; tsconfig.json here is also not composite). rimraf is not a dependency (deps: @foundryvtt/foundryvtt-cli, @modelcontextprotocol/sdk, dotenv, zod, biome, @types/node, esbuild, knip, playwright, typescript, vitest). And the sibling motivation is hypothetical today: 0 hits for `tools/molten` in fvtt-mod-battleflow / fxstudio / miscpatches / soundscape-sfx / fvtt-mcp-artificer, and because dist/ is gitignored the ghost exists only on the owner's machine — a clone never sees it.
CORRECTION: Fix: clean with a dependency-free `prebuild` (e.g. `node -e "require('fs').rmSync('dist',{recursive:true,force:true})"`) or add rimraf as a devDependency — drop the `tsc --build --clean` alternative, it leaves the ghost in place. Keep the asset-bridge.ts:8 comment fix and the keyword drop. Evidence should say the ghost is 9 files (js + d.ts + map) on the owner's box only, and that no sibling currently imports it.

## F49 [low] 51 of 151 tools are named by no skill; 6 of 8 placeable families are touched by at most one skill and only via list-*
VERDICT: corrected
REASON: Re-ran scratch/skills-matrix.mjs → tools=151 used=100 unused=51 (matches). Parsed the Tool→skills table in scratch/skills-tool-matrix.md: the 51 unnamed tools by toolset are world 4/8, actors 6/31, items 1/5, scenes 22/44, journals 2/11, combat 1/2, assets 9/12, organization 6/9 (macros 3 + folders 2 + bulk-delete) — every tool the finding lists is in the unnamed set, drawings 4 and macros 3 are all unnamed. Re-ran scratch/skills-repoint-size.mjs → 185 lines; actors 39, scenes 36, items 32, journals 27, tables 19, regions 17, cards 14, playlists 14, tokens 9, sounds/notes/folders 4, tiles/walls/lights 1 — matches. Two errors: (1) '22 of 45 scene tools' — dist/toolsets.js scenes = 44 and the matrix says 44, so 22 of 44. (2) '6 of 8 placeable families touched by at most one skill and only via list-*' is 4 of 8: tiles, lights, walls (scene-builder via list-* only) and drawings (no skill). sounds is named by 2 skills (playlist-builder, soundscape-builder) via create-sounds, not list; tokens by 4 skills via list-tokens + update-token (x3); notes by 2 skills naming all four tools; regions by 2 skills naming all seven. The author's own per-family table (scratch/skills-notes.md:36-43) shows exactly this. The skill-cheap conclusion survives only for tiles/walls/lights/drawings (1/1/1/0 mention lines).
CORRECTION: '22 of 44 scene tools' (not 45). '4 of 8 placeable families (tiles, lights, walls, drawings) are touched by ≤1 skill and only via list-*; sounds (2 skills via create-sounds), tokens (4 skills, list-tokens + update-token, 9 lines), notes (2 skills, all 4 tools) and regions (2 skills, all 7 tools, 17 lines) are not.' Fix text: the placeable consolidation is skill-cheap for those four families only; regions and tokens carry real re-point cost.

## F50 [low] House-style journal HTML re-derived in three places instead of the structuring tool
VERDICT: corrected
REASON: The three sites hold: src/tools/journal.ts:358-364 renders blocks with renderStyledHtml (src/tools/journal/blocks.ts:136-140 emits `<section class="mcp-journal"><style>…</style><div class="wrap">`); .claude/skills/session-scribe/SKILL.md:250-251 (bullet spans 244-254) hand-writes 'the mcp-journal format (p.lead TL;DR → h2.spaced → readaloud → ul)' into update-journal raw HTML; .claude/skills/bestiary-builder/bestiary-entry.mjs:186-190 hand-builds `<section class="mcp-journal"><div class="wrap">`. The session-scribe half of the fix is right and cheap: UpdateQuestJournalSchema (journal.ts:61-88) already takes `newPageName` + `playerVisible` + typed blocks, and heading/lead/readaloud/list are exactly the diary shape — a few SKILL.md lines. The bestiary half is not a 1-hour skill edit: the script upserts by page NAME and re-sorts every page alphabetically (bestiary-entry.mjs header steps 5-6, :200-215); update-quest-journal `newPageName` always creates and `pageId` appends (journal.ts:446-476), update-journal replaces content but needs a pageId from list-journals, and no journal tool sorts pages (src/page/journals.ts:321 appendPageSort only appends last). So the bestiary re-point is blocked on F4's tool work, which the finding only half-acknowledges ('after F4'). Also the script omits the inline `<style>` the renderer emits, so it is a third, differently-styled variant (it only looks right when another page's `<style>` leaks in the same sheet view), which strengthens the drift claim.
CORRECTION: Fix: session-scribe → update-quest-journal with `newPageName` + `playerVisible: true` + heading/lead/readaloud/list blocks (~1 hour, skill side). Bestiary → rides F4 (tool side): needs an upsert-by-page-name path and a page-sort capability (or an `html` block with `newPageName` plus a new sort option) before the script's hand-built wrapper can go; note the wrapper also drops the inline `<style>`. Exact lines: session-scribe/SKILL.md:250-251; bestiary-entry.mjs:186-190.
